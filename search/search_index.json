{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"IVCAP Service SDK for Python","text":"<p>Welcome to the IVCAP Service SDK \u2014 a Python library for building batch services that integrate with the IVCAP data and compute platform.</p> <p>This SDK simplifies development of long-running, queue-based worker services with minimal boilerplate. Focus on your business logic while the SDK handles:</p> <ul> <li>\u2705 Asynchronous job processing with automatic error handling</li> <li>\u2705 Artifact management \u2014 upload, download, and process files</li> <li>\u2705 Service composition \u2014 call other IVCAP services dynamically</li> <li>\u2705 Progress reporting \u2014 track job execution with named steps</li> <li>\u2705 Observability \u2014 export logs and metrics to OpenObserve</li> <li>\u2705 Type safety \u2014 fully typed with Pydantic models</li> </ul>"},{"location":"#quick-example","title":"Quick Example","text":"<pre><code>from pydantic import BaseModel, Field\nfrom ivcap_service import Service, ServiceContact, JobContext, start_batch_service, with_schema\n\nservice = Service(\n    name=\"My Service\",\n    contact=ServiceContact(name=\"Your Name\", email=\"you@example.com\"),\n)\n\n@with_schema(\"urn:sd:schema:my-service.request.1\")\nclass Request(BaseModel):\n    input_data: str = Field(description=\"Data to process\")\n\n@with_schema(\"urn:sd:schema:my-service.1\")\nclass Result(BaseModel):\n    output_data: str = Field(description=\"Processing result\")\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    with ctxt.report.step(\"processing\", msg=\"Starting work\") as step:\n        result = req.input_data.upper()\n        step.finished(msg=\"Done!\")\n    return Result(output_data=result)\n\nif __name__ == \"__main__\":\n    start_batch_service(service, process_job)\n</code></pre>"},{"location":"#getting-started","title":"Getting Started","text":"<p>New to IVCAP services? Start here:</p> <ol> <li>Installation \u2014 Set up the SDK</li> <li>Quick Start \u2014 Run your first service</li> <li>Your First Service \u2014 Build a complete example</li> </ol>"},{"location":"#learn-by-example","title":"Learn by Example","text":"<p>Check out the Examples section for complete, working services including:</p> <ul> <li>Batch processing with error handling</li> <li>Artifact upload/download workflows</li> <li>Service composition patterns</li> </ul>"},{"location":"#core-concepts","title":"Core Concepts","text":""},{"location":"#job-processing","title":"Job Processing","text":"<p>Every service processes jobs with strongly-typed inputs and outputs. Learn about: - Request/Result models (Pydantic) - Job context and metadata - Progress tracking with steps</p>"},{"location":"#ivcap-platform-integration","title":"IVCAP Platform Integration","text":"<p>Your service integrates with the platform through: - Artifact management (download inputs, upload results) - Service discovery and composition - Metadata attachment (aspects)</p>"},{"location":"#observability","title":"Observability","text":"<p>Monitor your services with: - Structured logging (exported to OpenObserve) - Progress events and job tracking - OpenTelemetry distributed tracing</p>"},{"location":"#next-steps","title":"Next Steps","text":"<ul> <li>Read Guides for deep dives into each feature</li> <li>Browse API Reference for complete class/function documentation</li> <li>Check Best Practices for production patterns</li> <li>Deploy with our Deployment Guide</li> </ul>"},{"location":"#deployment","title":"Deployment","text":"<p>Ready to deploy? The SDK works seamlessly with Docker and the IVCAP platform.</p> <pre><code># Test your service locally\npython my_service.py --test-file test_job.json\n\n# Print service metadata\npython my_service.py --print-service-description\n</code></pre> <p>See the Deployment Guide for production configurations.</p>"},{"location":"#where-to-find-help","title":"Where to Find Help","text":"<ul> <li>GitHub Issues: Report bugs or ask questions</li> <li>Contributing: Contributions welcome!</li> <li>Code of Conduct: Community standards</li> </ul>"},{"location":"#license","title":"License","text":"<p>This project is licensed under the MIT License.</p> <p>Happy building! \ud83d\ude80</p>"},{"location":"agent/","title":"Agent Guide","text":"<p>This page is the machine-readable quick-reference for <code>ivcap-service</code>, designed for AI coding assistants (Copilot, Cursor, Claude, Cline, \u2026).</p> <p>It is also available at the project root as <code>AGENTS.md</code> for agents that scan repository metadata before writing code.</p> <p>Also available in your repo</p> <p>When you clone this repository, <code>AGENTS.md</code> is at the project root so your IDE and any context-scanning agents can find it automatically.</p>"},{"location":"agent/#agentsmd-building-ivcap-batch-services","title":"AGENTS.md: Building IVCAP Batch Services","text":"<p>This document provides comprehensive instructions for agents (AGENTS) on how to use the ivcap-service library to build IVCAP batch services.</p>"},{"location":"agent/#overview","title":"Overview","text":"<p>The ivcap-service Python library simplifies development of batch services for the IVCAP platform. A batch service is a long-running worker that processes jobs in a queue-based manner, with each job being independently executed.</p>"},{"location":"agent/#quick-start-example","title":"Quick Start Example","text":"<p>The <code>examples/test-batch/batch_service.py</code> file demonstrates a complete batch service implementation. Refer to this file for a working example of all concepts described below.</p>"},{"location":"agent/#table-of-contents","title":"Table of Contents","text":"<ol> <li>Architecture</li> <li>Step-by-Step Guide</li> <li>1. Import Required Components</li> <li>2. Initialize Logging</li> <li>3. Define the Service</li> <li>4. Define the Request Model</li> <li>5. Define the Result Model</li> <li>6. Implement the Worker Function</li> <li>7. Using JobContext</li> <li>8. Interacting with IVCAP Services via context.ivcap</li> <li>9. Error Handling</li> <li>10. Logging</li> <li>11. Start the Service</li> <li>12. Testing</li> <li>13. Service Definition and Metadata</li> <li>Advanced Features</li> <li>Environment Variables</li> <li>Logging to OpenObserve</li> <li>OpenTelemetry Support</li> <li>Custom Arguments</li> <li>Project Structure</li> <li>Docker Deployment</li> <li>Complete Example Breakdown</li> <li>Best Practices</li> <li>Troubleshooting</li> <li>Related Documentation</li> </ol>"},{"location":"agent/#architecture","title":"Architecture","text":"<p>An IVCAP batch service consists of:</p> <ol> <li>Service Description - Metadata about the service (name, version, contact, license)</li> <li>Request Model - Pydantic model defining input parameters</li> <li>Result Model - Pydantic model defining output/result structure</li> <li>Worker Function - The main function that processes each job</li> <li>Entry Point - Bootstrap code that starts the service</li> </ol>"},{"location":"agent/#execution-flow","title":"Execution Flow","text":"<pre><code>IVCAP Platform\n    \u2193\nService Instance (waiting for work)\n    \u2193 (fetches next job)\nWorker Function (processes job)\n    \u2193\nResult/Error (reported back to platform)\n    \u2193\nRepeat (wait for next job)\n</code></pre>"},{"location":"agent/#step-by-step-guide","title":"Step-by-Step Guide","text":""},{"location":"agent/#1-import-required-components","title":"1. Import Required Components","text":"<pre><code>from pydantic import BaseModel, Field\nfrom ivcap_service import (\n    Service,\n    ServiceContact,\n    ServiceLicense,\n    JobContext,\n    getLogger,\n    logging_init,\n    start_batch_service,\n    with_schema,\n)\n</code></pre> <p>Key imports: - <code>Service</code> - Service descriptor class - <code>ServiceContact</code> - Typed contact details model (name, email, optional url) - <code>ServiceLicense</code> - Typed license model (name, optional url) - <code>JobContext</code> - Context object passed to worker function (optional) - <code>getLogger</code> - Structured logging - <code>logging_init</code> - Initialize logging system - <code>start_batch_service</code> - Bootstrap function to start the service - <code>with_schema</code> - Decorator to attach a <code>$schema</code> URN to a Pydantic model</p>"},{"location":"agent/#2-initialize-logging","title":"2. Initialize Logging","text":"<pre><code>logging_init()\nlogger = getLogger(\"app\")\n</code></pre> <p>This should be called at module level to set up the logging system. Use the returned logger throughout your code for consistent logging.</p>"},{"location":"agent/#3-define-the-service","title":"3. Define the Service","text":"<p>Create a <code>Service</code> object describing your service:</p> <pre><code>service = Service(\n    name=\"My Batch Service\",\n    version=os.environ.get(\"VERSION\", \"1.0.0\"),\n    contact=ServiceContact(\n        name=\"Your Name\",\n        email=\"your.email@example.com\",\n    ),\n    license=ServiceLicense(\n        name=\"MIT\",\n        url=\"https://opensource.org/license/MIT\",\n    ),\n)\n</code></pre> <p>Fields: - <code>name</code> (required) - Human-readable service name - <code>version</code> - Service version (commonly read from <code>VERSION</code> environment variable) - <code>contact</code> (required) - <code>ServiceContact</code> instance with <code>name</code> (str) and <code>email</code> (str) fields; optionally <code>url</code> (str) - <code>license</code> (optional) - <code>ServiceLicense</code> instance with <code>name</code> (str) and an optional <code>url</code> (str) pointing to the license text</p>"},{"location":"agent/#4-define-the-request-model","title":"4. Define the Request Model","text":"<p>Define a Pydantic model for job input parameters:</p> <pre><code>@with_schema(\"urn:sd:schema:my_service.request.1\")\nclass Request(BaseModel):\n    param1: str = Field(description=\"First parameter\")\n    param2: int = Field(default=10, description=\"Optional second parameter\")\n    param3: bool | None = Field(False, description=\"Optional boolean flag\")\n</code></pre> <p>Requirements: - Must inherit from <code>pydantic.BaseModel</code> - Must be decorated with <code>@with_schema(\"urn:sd:schema:...\")</code> \u2014 this injects the <code>$schema</code> field that IVCAP uses for data type identification - Do not manually add a <code>jschema</code> / <code>$schema</code> field; the decorator handles it - Every field should have a descriptive <code>description</code> for agent clarity - Use <code>Field()</code> to specify defaults and type information - Use type hints for all fields</p> <p>Schema URN Format: <code>urn:sd:schema:{service-name}.request.{version}</code> - Use a reverse-domain-like format for uniqueness - Include version number</p>"},{"location":"agent/#5-define-the-result-model","title":"5. Define the Result Model","text":"<p>Define a Pydantic model for job output/results:</p> <pre><code>@with_schema(\"urn:sd:schema:my_service.1\")\nclass Result(BaseModel):\n    result_field: str = Field(description=\"Main result\")\n    status: str = Field(description=\"Job status\")\n    execution_time: float = Field(description=\"Time in seconds\")\n</code></pre> <p>Requirements: - Must inherit from <code>pydantic.BaseModel</code> - Must be decorated with <code>@with_schema(\"urn:sd:schema:...\")</code> \u2014 this injects the <code>$schema</code> field - Do not manually add a <code>jschema</code> / <code>$schema</code> field; the decorator handles it - All fields should have descriptions - Should mirror the actual output your worker function produces</p> <p>Schema URN Format: <code>urn:sd:schema:{service-name}.{version}</code> - Similar to request but without <code>.request</code> suffix</p>"},{"location":"agent/#6-implement-the-worker-function","title":"6. Implement the Worker Function","text":"<p>The worker function is the core of your service:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    \"\"\"\n    Process a single job.\n\n    This comprehensive description helps agents understand what the service does\n    and when to use it. Describe the business logic, inputs, outputs, and any\n    side effects.\n\n    Args:\n        req: Request object containing job parameters\n        ctxt: JobContext with job metadata and reporting facilities\n\n    Returns:\n        Result object with computed results\n    \"\"\"\n    logger.info(f\"Processing job {ctxt.job_id}\")\n\n    # Your implementation here\n    result = perform_computation(req.param1, req.param2)\n\n    return Result(\n        result_field=result,\n        status=\"completed\",\n        execution_time=elapsed_time,\n    )\n</code></pre> <p>Function Signature Requirements: - First parameter MUST be the Request model type - Second parameter (optional) MUST be <code>ctxt: JobContext</code> if you need job context - Return type MUST be the Result model type - All type hints must be present (library uses them for validation)</p> <p>Function Behavior: - Called once per job by the service - Must return a Result instance (or raise an exception) - Has access to job context and reporting facilities</p>"},{"location":"agent/#7-using-jobcontext","title":"7. Using JobContext","text":"<p>The <code>JobContext</code> object provides access to job metadata and reporting:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    # Access job information\n    job_id = ctxt.job_id\n    logger.info(f\"Processing {job_id}\")\n\n    # Create a named execution step with reporting\n    with ctxt.report.step(\"data_preparation\", msg=\"Starting data prep\") as step:\n        # Do work here\n        prepare_data()\n        # Update step with completion message\n        step.finished(msg=\"Data preparation complete\")\n\n    # Create another step\n    with ctxt.report.step(\"processing\", msg=\"Processing data\") as step:\n        result = process_data()\n        step.finished(msg=\"Processing complete\")\n\n    return Result(result=result)\n</code></pre> <p>JobContext Properties: - <code>job_id</code> - Unique identifier for this job - <code>job_authorization</code> - Authorization token for downstream requests (for IVCAP platform calls) - <code>report</code> - EventReporter for reporting progress and events - <code>ivcap</code> - IVCAP client for platform interactions (lazy-initialized)</p> <p>EventReporter Methods:</p> <p>The <code>EventReporter</code> provides two ways to report events:</p> <ol> <li>Step Context Manager (recommended for progress tracking)</li> <li> <p><code>step(name, msg)</code> - Create a named step context manager for progress tracking</p> <ul> <li>Returns a context manager that yields a step object</li> <li>Call <code>step.finished(msg)</code> to mark completion within the context</li> <li>Use for long-running operations to provide progress updates</li> <li>Events are automatically reported when entering/exiting the context</li> </ul> </li> <li> <p>Direct Event Issuance (for immediate notifications)</p> </li> <li><code>step_started(name, msg)</code> - Report that a step has started</li> <li><code>step_info(name, msg)</code> - Report an informational update for a step</li> <li><code>step_error(name, error, context)</code> - Report an error in a step</li> <li><code>step_finished(name, msg)</code> - Report that a step has finished</li> <li>Use these methods when you need to issue events outside a context manager</li> <li>Useful for conditional reporting or async operations</li> </ol> <p>Example with direct event issuance:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    report = ctxt.report\n\n    # Start a step directly\n    report.step_started(\"initialization\", msg=\"Starting initialization\")\n\n    try:\n        result = initialize_system()\n        # Report progress updates\n        report.step_info(\"initialization\", msg=f\"Init progress: {result}%\")\n    except Exception as e:\n        # Report errors directly\n        report.step_error(\"initialization\", error=str(e), context=\"Failed to initialize\")\n        raise\n    finally:\n        # Finish the step\n        report.step_finished(\"initialization\", msg=\"Initialization complete\")\n\n    return Result(...)\n</code></pre> <p>This direct approach is particularly useful when: - Step duration is unknown or highly variable - Reporting conditional events based on runtime state - Working with async/await patterns where context managers are awkward - Needing fine-grained control over event timing</p>"},{"location":"agent/#8-interacting-with-ivcap-services-via-contextivcap","title":"8. Interacting with IVCAP Services via context.ivcap","text":"<p>The <code>JobContext</code> provides access to an already-instantiated <code>ivcap</code> client object, allowing your service to interact with other services, upload/download artifacts, and access platform capabilities without needing explicit authentication credentials.</p> <p>Authentication: The <code>ivcap</code> client is pre-configured with the platform-injected credentials and authorization headers. No JWT token or credentials are required when using <code>ctxt.ivcap</code> inside a job container.</p> <p>Common Operations:</p>"},{"location":"agent/#discover-and-invoke-other-services","title":"Discover and invoke other services","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    \"\"\"Call another IVCAP service from within this job\"\"\"\n    ivcap = ctxt.ivcap\n\n    # Find a service by name\n    service = ivcap.get_service_by_name(\"my-downstream-service\")\n\n    # Get the service's request model\n    ServiceReq = service.request_model\n\n    # Invoke the service and wait for completion\n    downstream_job = service.request_job(\n        ServiceReq(param1=\"value\", param2=42),\n        timeout=300  # wait up to 5 minutes\n    )\n\n    # Access the result\n    if downstream_job.succeeded:\n        result = downstream_job.result\n        logger.info(f\"Downstream service result: {result}\")\n    else:\n        logger.error(f\"Downstream service failed: {downstream_job.status()}\")\n\n    return Result(...)\n</code></pre> <p>This enables service composition \u2014 your service can orchestrate workflows by chaining calls to other IVCAP services.</p>"},{"location":"agent/#upload-artifacts","title":"Upload artifacts","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    \"\"\"Generate output and upload as artifacts\"\"\"\n    ivcap = ctxt.ivcap\n\n    # Generate some output\n    output_data = b\"result data here\"\n\n    # Upload as an artifact\n    artifact = ivcap.upload_artifact(\n        name=\"my-result.json\",\n        io_stream=io.BytesIO(output_data),\n        content_type=\"application/json\",\n        content_size=len(output_data),\n    )\n\n    logger.info(f\"Uploaded artifact: {artifact.id}\")\n\n    return Result(artifact_urn=artifact.id)\n</code></pre> <p>Artifacts uploaded by the service are automatically linked to the job in the Datafabric via provenance aspects.</p>"},{"location":"agent/#download-and-read-input-artifacts","title":"Download and read input artifacts","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    \"\"\"Download an input artifact and process it\"\"\"\n    ivcap = ctxt.ivcap\n\n    # Get artifact by URN (passed as a service parameter)\n    artifact = ivcap.get_artifact(req.input_artifact_urn)\n\n    # Stream to a local file\n    with open(\"/tmp/input.csv\", \"wb\") as f:\n        for chunk in artifact.as_stream():\n            f.write(chunk)\n\n    # Or use as a temporary file (auto-deleted)\n    with artifact.as_local_file() as path:\n        with open(path, \"r\") as f:\n            data = f.read()\n\n    logger.info(f\"Processed artifact: {artifact.name} ({artifact.size} bytes)\")\n    return Result(...)\n</code></pre>"},{"location":"agent/#query-and-attach-metadata-aspects","title":"Query and attach metadata (Aspects)","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    \"\"\"Add domain-specific metadata to artifacts\"\"\"\n    ivcap = ctxt.ivcap\n\n    # Get an artifact\n    artifact = ivcap.get_artifact(req.artifact_urn)\n\n    # List existing metadata/aspects\n    for aspect in ivcap.list_aspects(entity=artifact.id):\n        logger.info(f\"Aspect schema: {aspect.schema}\")\n\n    # Attach custom metadata\n    annotation = ivcap.add_aspect(\n        entity=artifact.id,\n        aspect={\n            \"$schema\": \"urn:my-service:schema:processing-result.1\",\n            \"processed_by\": \"my-service\",\n            \"processing_time_seconds\": 42.5,\n            \"status\": \"success\",\n            \"tags\": [\"important\", \"verified\"],\n        },\n    )\n\n    logger.info(f\"Added annotation: {annotation.id}\")\n    return Result(...)\n</code></pre> <p>Metadata added to artifacts in the Datafabric is queryable and enables rich data discovery and lineage tracking.</p>"},{"location":"agent/#list-services-and-filter","title":"List services and filter","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    \"\"\"Discover available services dynamically\"\"\"\n    ivcap = ctxt.ivcap\n\n    # List all available services\n    for service in ivcap.list_services(limit=50):\n        logger.info(f\"Service: {service.name} (v{service.version})\")\n\n    # Find services by name pattern\n    service = ivcap.get_service_by_name(\"image-processor\")\n\n    # Inspect parameters\n    for name, param in service.parameters.items():\n        print(f\"  {name}: {param.type}, optional={param.is_optional}\")\n\n    return Result(...)\n</code></pre> <p>This allows your service to dynamically discover what other capabilities are available on the platform.</p>"},{"location":"agent/#handle-errors-gracefully","title":"Handle errors gracefully","text":"<pre><code>from ivcap_client.exception import ResourceNotFound, IvcapApiError\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    \"\"\"Handle errors when interacting with platform services\"\"\"\n    ivcap = ctxt.ivcap\n\n    try:\n        service = ivcap.get_service_by_name(\"optional-service\")\n        job = service.request_job(req, timeout=60)\n        result = job.result\n    except ResourceNotFound as e:\n        logger.warning(f\"Service not found: {e.resource}\")\n        # Gracefully degrade \u2014 use fallback logic\n        result = fallback_implementation()\n    except IvcapApiError as e:\n        logger.error(f\"Platform API error [{e.status_code}]: {e}\")\n        raise\n\n    return Result(...)\n</code></pre>"},{"location":"agent/#monitor-jobs-asynchronously","title":"Monitor jobs asynchronously","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    \"\"\"Submit multiple jobs and monitor them\"\"\"\n    ivcap = ctxt.ivcap\n\n    # Dispatch multiple downstream jobs\n    service = ivcap.get_service_by_name(\"batch-processor\")\n    jobs = []\n    for item in req.items:\n        Model = service.request_model\n        job = service.request_job(Model(data=item), timeout=0)  # don't wait\n        jobs.append(job)\n\n    logger.info(f\"Dispatched {len(jobs)} jobs\")\n\n    # Monitor them\n    completed = []\n    while len(completed) &lt; len(jobs):\n        for job in jobs:\n            if job not in completed:\n                job.refresh()\n                if job.finished:\n                    logger.info(f\"Job {job.id} finished with status {job.status()}\")\n                    completed.append(job)\n        time.sleep(5)\n\n    return Result(job_count=len(jobs), all_succeeded=all(j.succeeded for j in jobs))\n</code></pre> <p>Key Benefits of Using context.ivcap:</p> <ol> <li>No credential management - Already authenticated with platform context</li> <li>Service composition - Chain services together for complex workflows</li> <li>Data lineage - Artifacts and metadata are automatically linked via provenance</li> <li>Dynamic discovery - Find and use available services at runtime</li> <li>Rich metadata - Attach domain-specific aspects to any entity</li> <li>Error handling - Graceful degradation when platform operations fail</li> </ol> <p>Common Patterns:</p> Pattern Use Case Service chaining Invoke sequential analysis steps Map-reduce Dispatch parallel jobs, aggregate results Conditional branching Route to different services based on data Fallback logic Try primary service, use alternative if unavailable Result aggregation Combine outputs from multiple jobs Metadata enrichment Add classifications, tags, provenance info <p>For more details on IVCAP client capabilities, refer to the ivcap-client SDK documentation.</p>"},{"location":"agent/#9-error-handling","title":"9. Error Handling","text":"<p>Exceptions in the worker function are caught and converted to <code>ExecutionError</code>:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    if req.param1 &lt; 0:\n        raise ValueError(\"param1 must be positive\")\n\n    if req.param2 &gt; 100:\n        raise ValueError(\"param2 cannot exceed 100\")\n\n    return Result(result=\"success\")\n</code></pre> <p>Error Handling Behavior: - Unhandled exceptions are automatically converted to <code>ExecutionError</code> - The error type, message, and traceback are preserved - The job is marked as failed on the platform - The service continues processing the next job</p>"},{"location":"agent/#10-logging","title":"10. Logging","text":"<p>Use the logger throughout your code:</p> <pre><code>logger.debug(\"Detailed debug information\")\nlogger.info(\"Important milestones\")\nlogger.warning(\"Unexpected but recoverable issues\")\nlogger.error(\"Serious errors (but doesn't stop the service)\")\n</code></pre> <p>Logging Best Practices: - Initialize with <code>logging_init()</code> at module level - Use <code>getLogger(\"component_name\")</code> for module-specific loggers - Log important state transitions and milestones - Include context (job_id, etc.) in log messages - Use appropriate log levels</p>"},{"location":"agent/#11-start-the-service","title":"11. Start the Service","text":"<p>At the bottom of your service file:</p> <pre><code>if __name__ == \"__main__\":\n    start_batch_service(service, process_job)\n</code></pre> <p>Function Signature: <pre><code>start_batch_service(\n    service_description: Service,\n    worker_fn: Callable[..., Any],\n    *,\n    custom_args: Callable[[argparse.ArgumentParser], argparse.Namespace] | None = None,\n    run_opts: dict[str, Any] | None = None,\n    with_telemetry: bool | None = None,\n)\n</code></pre></p> <p>Parameters: - <code>service_description</code> - The Service object created earlier - <code>worker_fn</code> - The worker function to call for each job - <code>custom_args</code> (optional) - Function to add custom command-line arguments - <code>run_opts</code> (optional) - Additional runtime options - <code>with_telemetry</code> (optional) - Enable OpenTelemetry instrumentation</p> <p>Command-Line Options (automatic): - <code>--print-service-description</code> - Print service metadata - <code>--print-tool-description</code> - Print tool schema - <code>--test-file &lt;path&gt;</code> - Test service with a job file (useful for debugging) - <code>--with-telemetry</code> - Enable OpenTelemetry tracing</p>"},{"location":"agent/#12-testing","title":"12. Testing","text":"<p>Test your service locally using a job file:</p> <pre><code>{\n  \"id\": \"urn:ivcap:job:test-123\",\n  \"in-content-type\": \"application/json\",\n  \"in-content\": {\n    \"param1\": \"value1\",\n    \"param2\": 42\n  }\n}\n</code></pre> <p>Run with test file: <pre><code>python my_service.py --test-file job.json\n</code></pre></p> <p>This executes your worker function once with the provided input and prints the result.</p>"},{"location":"agent/#13-service-definition-and-metadata","title":"13. Service Definition and Metadata","text":"<p>To view generated service metadata:</p> <pre><code>python my_service.py --print-service-description\npython my_service.py --print-tool-description\n</code></pre> <p>These print JSON schemas and metadata that IVCAP uses to: - Understand service capabilities - Display in UI - Route jobs appropriately - Help agents understand when to use the service</p>"},{"location":"agent/#advanced-features","title":"Advanced Features","text":""},{"location":"agent/#environment-variables","title":"Environment Variables","text":"<p>Common environment variables used by the SDK:</p> <ul> <li><code>VERSION</code> - Service version (used if not provided to Service constructor)</li> <li><code>IVCAP_BASE_URL</code> - IVCAP platform URL (for job fetching and result reporting)</li> <li><code>OPENOBSERVE_URL</code> - OpenObserve endpoint for log/metric export</li> <li><code>OPENOBSERVE_ORG</code> - OpenObserve organization</li> <li><code>OPENOBSERVE_USERNAME</code> - OpenObserve username</li> <li><code>OPENOBSERVE_TOKEN</code> - OpenObserve authentication token</li> <li><code>OTEL_EXPORTER_OTLP_ENDPOINT</code> - OpenTelemetry endpoint</li> </ul>"},{"location":"agent/#logging-to-openobserve","title":"Logging to OpenObserve","text":"<p>The SDK automatically exports logs and metrics to OpenObserve when configured:</p> <pre><code>export OPENOBSERVE_URL=\"https://observe.example.com\"\nexport OPENOBSERVE_ORG=\"myorg\"\nexport OPENOBSERVE_USERNAME=\"user@example.com\"\nexport OPENOBSERVE_TOKEN=\"token\"\n</code></pre> <p>All logs via <code>getLogger()</code> are automatically sent to OpenObserve.</p>"},{"location":"agent/#opentelemetry-support","title":"OpenTelemetry Support","text":"<p>For distributed tracing, enable with:</p> <pre><code>python my_service.py --with-telemetry\n</code></pre> <p>And configure the OTEL endpoint:</p> <pre><code>export OTEL_EXPORTER_OTLP_ENDPOINT=\"http://otel-collector:4318\"\n</code></pre> <p>The SDK automatically: - Creates spans for each job execution - Records job outcomes (success/error) - Instruments HTTP requests/responses - Records execution duration and errors</p>"},{"location":"agent/#custom-arguments","title":"Custom Arguments","text":"<p>Add custom command-line arguments:</p> <pre><code>def add_custom_args(parser):\n    parser.add_argument('--custom-option', type=str, help='Custom option')\n    return parser.parse_args()\n\nif __name__ == \"__main__\":\n    start_batch_service(\n        service,\n        process_job,\n        custom_args=add_custom_args,\n    )\n</code></pre>"},{"location":"agent/#project-structure","title":"Project Structure","text":"<p>A typical IVCAP batch service project layout:</p> <pre><code>my_service/\n\u251c\u2500\u2500 pyproject.toml           # Poetry configuration\n\u251c\u2500\u2500 poetry.lock              # Dependencies lock file\n\u251c\u2500\u2500 Dockerfile               # Docker image for deployment\n\u251c\u2500\u2500 my_service.py            # Main service code\n\u251c\u2500\u2500 tests/\n\u2502   \u251c\u2500\u2500 job1.json           # Test job files\n\u2502   \u2514\u2500\u2500 job2.json\n\u251c\u2500\u2500 requirements.txt         # Optional pip requirements\n\u2514\u2500\u2500 README.md               # Service documentation\n</code></pre>"},{"location":"agent/#pyprojecttoml-configuration","title":"pyproject.toml Configuration","text":"<p>Key sections for IVCAP services:</p> <pre><code>[project]\nname = \"my-service\"\nversion = \"1.0.0\"\ndescription = \"My batch service\"\n\n[project.dependencies]\npython = \"&gt;=3.10,&lt;4.0\"\npydantic = \"&gt;=2.0\"\nivcap_service = \"^0.6.3\"\n\n[tool.poetry-plugin-ivcap]\nservice-file = \"my_service.py\"\nservice-id = \"urn:ivcap:service:unique-uuid\"\nservice-type = \"batch\"\n</code></pre>"},{"location":"agent/#docker-deployment","title":"Docker Deployment","text":"<p>Example Dockerfile:</p> <pre><code>FROM python:3.11-slim\n\nWORKDIR /app\n\n# Copy service code\nCOPY my_service.py .\nCOPY pyproject.toml .\nCOPY poetry.lock .\n\n# Install dependencies\nRUN pip install poetry &amp;&amp; poetry install --no-dev\n\nENV VERSION=\"1.0.0\"\nENV IVCAP_BASE_URL=\"http://ivcap.local\"\n\nCMD [\"python\", \"my_service.py\"]\n</code></pre> <p>Build and run:</p> <pre><code>docker build -t my-service .\ndocker run -e IVCAP_BASE_URL=\"http://ivcap.local\" my-service\n</code></pre>"},{"location":"agent/#complete-example-breakdown","title":"Complete Example Breakdown","text":"<p>The <code>examples/test-batch/batch_service.py</code> demonstrates:</p> <ol> <li>Service Definition - Describes a CPU load testing service</li> <li>Request Model - Parameters for duration, CPU %, and error injection</li> <li>Result Model - Returns execution time and status</li> <li>Worker Function - <code>consume_compute()</code> performs the actual work</li> <li>Error Handling - Gracefully handles exceptions, exit codes, OOM</li> <li>Progress Reporting - Uses <code>ctxt.report.step()</code> to track execution</li> <li>Logging - Logs important milestones and debug info</li> <li>Bootstrap - Uses <code>start_batch_service()</code> to run</li> </ol> <p>Key patterns used:</p> <ul> <li>Step-based progress: Wraps work in named steps for tracking</li> <li>Comprehensive docstrings: Explains what the service does</li> <li>Flexible error modes: Tests exception handling, exit codes, OOM errors</li> <li>Resource configuration: Demonstrates parameter ranges and types</li> </ul>"},{"location":"agent/#best-practices","title":"Best Practices","text":""},{"location":"agent/#1-request-and-result-models","title":"1. Request and Result Models","text":"<ul> <li>Use <code>@with_schema(...)</code> decorator - Always decorate models with <code>@with_schema(\"urn:sd:schema:...\")</code> instead of adding a manual <code>jschema</code> field</li> <li>Use descriptive field names - Make it clear what each field represents</li> <li>Provide comprehensive descriptions - Agents use these to understand inputs/outputs</li> <li>Use appropriate types - <code>int</code>, <code>str</code>, <code>bool</code>, <code>list</code>, <code>dict</code>, etc.</li> <li>Set sensible defaults - Use <code>Field(default=...)</code> for optional parameters</li> <li>Validate data - Use Pydantic validators for complex constraints</li> </ul>"},{"location":"agent/#2-worker-function","title":"2. Worker Function","text":"<ul> <li>Keep it focused - Process one job at a time</li> <li>Use meaningful names - <code>process_job</code>, <code>analyze_data</code>, <code>generate_report</code></li> <li>Add comprehensive docstring - Explain what the service does and when to use it</li> <li>Log key milestones - Help users and developers understand execution</li> <li>Use steps for progress - Wrap long operations in <code>ctxt.report.step()</code></li> <li>Handle errors gracefully - Raise informative exceptions</li> </ul>"},{"location":"agent/#3-deployment","title":"3. Deployment","text":"<ul> <li>Version your service - Use semantic versioning</li> <li>Containerize properly - Use Docker for reproducible deployments</li> <li>Configure environment variables - Document all required and optional env vars</li> <li>Test before deploying - Use <code>--test-file</code> to verify locally</li> <li>Monitor execution - Enable logging and OpenObserve for observability</li> </ul>"},{"location":"agent/#4-documentation","title":"4. Documentation","text":"<ul> <li>Write detailed service descriptions - Explain business logic and use cases</li> <li>Document all parameters - In model field descriptions</li> <li>Provide example inputs - Include sample request JSON files</li> <li>List dependencies - In pyproject.toml and documentation</li> <li>Document deployment requirements - Resource limits, environment variables</li> </ul>"},{"location":"agent/#troubleshooting","title":"Troubleshooting","text":""},{"location":"agent/#service-wont-start","title":"Service won't start","text":"<ul> <li>Check <code>IVCAP_BASE_URL</code> environment variable</li> <li>Ensure IVCAP platform is accessible</li> <li>Check logs for connection errors</li> </ul>"},{"location":"agent/#jobs-not-being-processed","title":"Jobs not being processed","text":"<ul> <li>Verify service is running: <code>docker logs &lt;container&gt;</code></li> <li>Check IVCAP platform job queue</li> <li>Ensure service description is registered</li> </ul>"},{"location":"agent/#incorrect-results","title":"Incorrect results","text":"<ul> <li>Run with <code>--test-file</code> to debug locally</li> <li>Check Request model matches actual data</li> <li>Verify Result model serialization</li> </ul>"},{"location":"agent/#memory-or-performance-issues","title":"Memory or performance issues","text":"<ul> <li>Profile worker function</li> <li>Check for unbounded loops or memory leaks</li> <li>Monitor logs and metrics via OpenObserve</li> </ul>"},{"location":"agent/#related-documentation","title":"Related Documentation","text":"<ul> <li>IVCAP Platform Documentation</li> <li>Pydantic Documentation</li> <li>Python asyncio (for async services)</li> <li>OpenTelemetry Python</li> </ul>"},{"location":"api/events/","title":"Events &amp; Reporting","text":"<p>Classes and utilities for reporting job progress and events.</p> Copy Markdown"},{"location":"api/events/#ivcap_service.events","title":"<code>events</code>","text":""},{"location":"api/events/#ivcap_service.events.EventContext","title":"<code>EventContext</code>","text":"Source code in <code>ivcap_service/events.py</code> <pre><code>class EventContext:\n    def __init__(\n        self,\n        event_name: str,\n        reporter: \"EventReporter\",\n        finishEventClass: type[GenericEvent] | None,\n        errorEventClass: type[GenericErrorEvent] | None,\n    ):\n        self._event_name = event_name\n        self._reporter = reporter\n        self._finishEventClass = finishEventClass\n        self._errorEventClass = errorEventClass\n        self._finished_sent = False\n\n        # Best-effort OpenTelemetry child span for the event scope.\n        # This should never break services that don't use OTEL.\n        self._otel_span_cm = None\n        self._otel_span = None\n        try:\n            from opentelemetry import trace\n\n            tracer = trace.get_tracer(\"ivcap_service.events\")\n            self._otel_span_cm = tracer.start_as_current_span(\n                f\"ivcap.event:{event_name}\"\n            )\n            self._otel_span = self._otel_span_cm.__enter__()\n            try:\n                self._otel_span.set_attribute(\"ivcap.job_id\", reporter.job_id)\n                self._otel_span.set_attribute(\"ivcap.event_name\", event_name)\n            except Exception:\n                pass\n        except Exception:\n            pass\n\n    @property\n    def name(self):\n        return self._event_name\n\n    def finished(self, message=None, **kwargs):\n        options = kwargs\n        if message:\n            options[\"message\"] = message\n        if self._finishEventClass:\n            event = self._finishEventClass(name=self._event_name, options=options)\n        else:\n            event = GenericEvent(name=self._event_name, options=options)\n        self._reporter.emit(event)\n        self._finished_sent = True\n\n        if self._otel_span is not None:\n            try:\n                from opentelemetry.trace import Status, StatusCode\n\n                self._otel_span.set_status(Status(StatusCode.OK))\n            except Exception:\n                pass\n\n    def info(self, event: BaseEvent | dict):\n        # We keep this ergonomic for callers: if they pass a raw dict,\n        # wrap it into a generic event.\n        if isinstance(event, BaseEvent):\n            self._reporter.emit(event)\n        else:\n            self._reporter.emit(GenericEvent(name=self._event_name, options=event))\n\n    def error(self, err: Exception, context: str | None = None):\n        evc = (\n            self._errorEventClass\n            if self._errorEventClass is not None\n            else GenericErrorEvent\n        )\n        stacktrace = traceback.format_tb(err.__traceback__)\n        if not context:\n            context = self._event_name\n        event = evc(error=str(err), stacktrace=stacktrace, context=context)\n        self._reporter.emit(event)\n\n        if self._otel_span is not None:\n            try:\n                self._otel_span.record_exception(err)\n            except Exception:\n                pass\n            try:\n                from opentelemetry.trace import Status, StatusCode\n\n                self._otel_span.set_status(\n                    Status(StatusCode.ERROR, description=str(err))\n                )\n            except Exception:\n                pass\n\n    def close(self):\n        \"\"\"Close the underlying OTEL span if one was created.\"\"\"\n\n        if self._otel_span_cm is not None:\n            try:\n                self._otel_span_cm.__exit__(None, None, None)\n            except Exception:\n                pass\n            finally:\n                self._otel_span_cm = None\n                self._otel_span = None\n\n    def __del__(self):\n        # Best-effort cleanup in case callers forget to exit the context.\n        try:\n            self.close()\n        except Exception:\n            pass\n</code></pre>"},{"location":"api/events/#ivcap_service.events.EventContext.close","title":"<code>close()</code>","text":"<p>Close the underlying OTEL span if one was created.</p> Source code in <code>ivcap_service/events.py</code> <pre><code>def close(self):\n    \"\"\"Close the underlying OTEL span if one was created.\"\"\"\n\n    if self._otel_span_cm is not None:\n        try:\n            self._otel_span_cm.__exit__(None, None, None)\n        except Exception:\n            pass\n        finally:\n            self._otel_span_cm = None\n            self._otel_span = None\n</code></pre>"},{"location":"api/events/#ivcap_service.events.set_event_reporter_factory","title":"<code>set_event_reporter_factory(factory)</code>","text":"<p>Set a factory function for creating specialised EventReporter instances.</p> <p>Parameters:</p> Name Type Description Default <code>factory</code> <code>EventFactoryF | None</code> <p>A factory function that takes a job ID and an optional job authorization token,</p> required Source code in <code>ivcap_service/events.py</code> <pre><code>def set_event_reporter_factory(factory: EventFactoryF | None):\n    \"\"\"\n    Set a factory function for creating specialised EventReporter instances.\n\n    Args:\n        factory (EventFactoryF | None): A factory function that takes a job ID and an optional job authorization token,\n        and returns an instance of EventReporter. If None, the default EventReporter will be used.\n    \"\"\"\n    global event_reporter_factory\n    if factory is not None and not callable(factory):\n        raise ValueError(\n            \"Factory must be a callable that returns an EventReporter instance.\"\n        )\n    event_reporter_factory = factory\n</code></pre>"},{"location":"api/events/#ivcap_service.events.create_event_reporter","title":"<code>create_event_reporter(job_id, job_authorization=None)</code>","text":"<p>Create an EventReporter instance for the given job ID.</p> <p>Parameters:</p> Name Type Description Default <code>job_id</code> <code>str</code> <p>The unique identifier for the job.</p> required <code>job_authorization</code> <code>Optional[str]</code> <p>Optional authorization token for the job.</p> <code>None</code> <p>Returns:</p> Name Type Description <code>EventReporter</code> <code>EventReporter</code> <p>An instance of EventReporter initialized with the job ID.</p> Source code in <code>ivcap_service/events.py</code> <pre><code>def create_event_reporter(\n    job_id: str, job_authorization: str | None = None\n) -&gt; \"EventReporter\":\n    \"\"\"\n    Create an EventReporter instance for the given job ID.\n\n\n    Args:\n        job_id (str): The unique identifier for the job.\n        job_authorization (Optional[str]): Optional authorization token for the job.\n\n    Returns:\n        EventReporter: An instance of EventReporter initialized with the job ID.\n    \"\"\"\n    if event_reporter_factory is not None:\n        return event_reporter_factory(job_id, job_authorization)\n    return EventReporter(job_id, job_authorization)\n</code></pre>"},{"location":"api/job-context/","title":"JobContext","text":"<p><code>JobContext</code> is the gateway to the IVCAP platform from within your job worker function. It is passed automatically as the second argument to every worker function and provides:</p> <ul> <li>Job identity \u2014 the unique ID and platform-injected authorisation token for the current job.</li> <li>Progress &amp; event reporting \u2014 a pre-configured <code>EventReporter</code> for emitting structured step events back to the platform.</li> <li>Full IVCAP platform access \u2014 a lazy-initialized, pre-authenticated <code>IVCAP</code> client from the ivcap-client library for uploading/downloading artifacts, querying metadata, and calling other services.</li> </ul> <pre><code>from ivcap_service import JobContext\n\ndef process_job(req: MyRequest, ctxt: JobContext) -&gt; MyResult:\n    # Identity\n    logger.info(f\"Processing job {ctxt.job_id}\")\n\n    # Progress reporting\n    with ctxt.report.step(\"analysis\", msg=\"Analysing input\") as step:\n        result = analyse(req.data)\n        step.finished(msg=\"Analysis complete\")\n\n    # Platform access\n    artifact = ctxt.ivcap.get_artifact(req.input_urn)\n    with artifact.as_local_file() as path:\n        data = path.read_bytes()\n\n    return MyResult(output=result)\n</code></pre>"},{"location":"api/job-context/#properties","title":"Properties","text":""},{"location":"api/job-context/#job_id-str","title":"<code>job_id</code> \u00b7 <code>str</code>","text":"<p>The platform-assigned unique identifier for this job (e.g. <code>urn:ivcap:job:...</code>). Use this in log messages and when correlating events or results.</p> <pre><code>logger.info(f\"Starting job {ctxt.job_id}\")\n</code></pre>"},{"location":"api/job-context/#report-eventreporter","title":"<code>report</code> \u00b7 <code>EventReporter</code>","text":"<p>A fully configured <code>EventReporter</code> instance for emitting structured progress events during execution. The reporter uses the platform sidecar when running on IVCAP, and falls back to structured logging locally.</p> <p>Quick reference:</p> <pre><code># Context-manager style (recommended)\nwith ctxt.report.step(\"load\", msg=\"Loading data\") as step:\n    data = load(req.source)\n    step.finished(msg=f\"Loaded {len(data)} records\")\n\n# Direct-call style (for async or conditional reporting)\nctxt.report.step_started(\"analysis\", msg=\"Starting analysis\")\nctxt.report.step_info(\"analysis\", msg=\"50% done\")       # (1)\nctxt.report.step_finished(\"analysis\", msg=\"Done\")\n</code></pre> <ol> <li><code>step_info</code> is called <code>step_info</code> in the <code>EventReporter</code>; it takes <code>step_name</code> and <code>msg</code>.</li> </ol> <p>For full details on event types, the <code>EventContext</code> step object, and custom reporters see Events &amp; Reporting API \u2192</p>"},{"location":"api/job-context/#ivcap-ivcap","title":"<code>ivcap</code> \u00b7 <code>IVCAP</code>","text":"<p>A lazy-initialized, pre-authenticated client from the <code>ivcap-client</code> library. It is instantiated on first access \u2014 no credentials or configuration are required inside a platform job container.</p> <pre><code>ivcap = ctxt.ivcap   # property access, no parentheses\n</code></pre> <p>The client is the primary entry point for all platform interactions:</p> What you want to do Method / starting point Download an input artifact <code>ivcap.get_artifact(urn)</code> Upload a result artifact <code>ivcap.upload_artifact(...)</code> Attach metadata to an entity <code>ivcap.add_aspect(entity, aspect)</code> List or query existing aspects <code>ivcap.list_aspects(entity=...)</code> Call another IVCAP service <code>ivcap.get_service_by_name(...)</code> \u2192 <code>.request_job(...)</code> List available services <code>ivcap.list_services(...)</code> <p>Working with artifacts \u2192 Artifacts Guide</p> <p>Calling other services \u2192 Service Composition Guide</p>"},{"location":"api/job-context/#job_authorization-str-none","title":"<code>job_authorization</code> \u00b7 <code>str | None</code>","text":"<p>The Bearer token injected by the platform for the current job. This is used internally by the SDK to authenticate outbound HTTP calls (via the <code>requests</code> and <code>httpx</code> monkey-patches in <code>context.py</code>) and by the <code>EventReporter</code> when posting events to the sidecar.</p> <p>You rarely need to access this directly \u2014 the <code>ivcap</code> client and the HTTP instrumentation layer use it automatically.</p>"},{"location":"api/job-context/#execution-flow","title":"Execution flow","text":"<pre><code>sequenceDiagram\n    participant P as IVCAP Platform\n    participant S as Service Worker\n    participant R as EventReporter\n    participant C as IVCAP Client\n\n    P-&gt;&gt;S: invoke worker(req, ctxt)\n    S-&gt;&gt;R: ctxt.report.step(\"\u2026\")\n    R-&gt;&gt;P: POST /events/{job_id}\n    S-&gt;&gt;C: ctxt.ivcap.get_artifact(\u2026)\n    C-&gt;&gt;P: GET /artifacts/{id}\n    P--&gt;&gt;C: artifact stream\n    S-&gt;&gt;R: step.finished(\u2026)\n    R-&gt;&gt;P: POST /events/{job_id}\n    S-&gt;&gt;P: result (via push_result)</code></pre>"},{"location":"api/job-context/#api-reference","title":"API Reference","text":"Copy Markdown"},{"location":"api/job-context/#ivcap_service.types.JobContext","title":"<code>JobContext</code>","text":"<p>             Bases: <code>BaseModel</code></p> Source code in <code>ivcap_service/types.py</code> <pre><code>class JobContext(BaseModel):\n    job_id: str\n    report: EventReporter\n    job_authorization: str | None = None\n\n    _ivcap: IVCAP | None = PrivateAttr(None)\n\n    model_config = ConfigDict(arbitrary_types_allowed=True)\n\n    @property\n    def ivcap(self) -&gt; IVCAP:\n        if self._ivcap is None:\n            self._ivcap = cast(IVCAP, IVCAP())\n        return self._ivcap\n</code></pre>"},{"location":"api/overview/","title":"API Reference Overview","text":"<p>The API reference documentation is automatically generated from the Python source code docstrings using mkdocstrings. This ensures the documentation stays in sync with the actual code.</p>"},{"location":"api/overview/#main-classes","title":"Main Classes","text":""},{"location":"api/overview/#service","title":"Service","text":"<p>Defines service metadata, contact information, and configuration. Used to describe your service to the IVCAP platform.</p> <p>See: Service</p>"},{"location":"api/overview/#jobcontext","title":"JobContext","text":"<p>Provides access to job metadata, progress reporting, and IVCAP platform APIs during job execution. Passed to your job processing function.</p> <p>See: JobContext</p>"},{"location":"api/overview/#events-reporting","title":"Events &amp; Reporting","text":"<p>Classes for reporting job progress, steps, and events back to the platform.</p> <p>See: Events &amp; Reporting</p>"},{"location":"api/overview/#additional-resources","title":"Additional Resources","text":"<ul> <li>Types: Common type definitions and data models</li> <li>Utilities: Helper functions for logging and common operations</li> </ul>"},{"location":"api/overview/#documentation-style","title":"Documentation Style","text":"<p>All API documentation follows Google-style docstrings for consistency: - Clear parameter descriptions - Return type documentation - Usage examples in docstrings - Attribute documentation</p> <p>The documentation is generated programmatically from the source code, not written manually, so it always reflects the current implementation.</p>"},{"location":"api/service/","title":"Service","text":"<p>This page documents the core <code>Service</code> class used to define service metadata and configuration.</p> Copy Markdown"},{"location":"api/service/#ivcap_service.service_definition.Service","title":"<code>Service</code>","text":"<p>             Bases: <code>BaseModel</code></p> Source code in <code>ivcap_service/service.py</code> <pre><code>class Service(BaseModel):\n    name: str = Field(description=\"name of the service\")\n    version: str | None = Field(\n        os.environ.get(\"VERSION\", \"???\"), description=\"version of the service\"\n    )\n    contact: ServiceContact = Field(description=\"contact details of the service\")\n    license: ServiceLicense | None = Field(None, description=\"license of the service\")\n</code></pre>"},{"location":"api/types/","title":"Types","text":"<p>Core type definitions and data models used throughout the SDK.</p> Copy Markdown"},{"location":"api/types/#ivcap_service.types","title":"<code>types</code>","text":""},{"location":"api/types/#ivcap_service.types.BinaryResult","title":"<code>BinaryResult</code>  <code>dataclass</code>","text":"<p>If the result of the tool is a non json serialisable object, return an instance of this class indicating the content-type and the actual result either as a byte array or a file handle to a binary content (<code>open(..., \"rb\")</code>)</p> Source code in <code>ivcap_service/types.py</code> <pre><code>@dataclass\nclass BinaryResult:\n    \"\"\"If the result of the tool is a non json serialisable object, return an\n    instance of this class indicating the content-type and the actual\n    result either as a byte array or a file handle to a binary content (`open(..., \"rb\")`)\"\"\"\n\n    content_type: str = Field(description=\"Content type of result serialised\")\n    content: bytes | str | BinaryIO = Field(\n        description=\"Content to send, either as byte array or file handle\"\n    )\n</code></pre>"},{"location":"api/types/#ivcap_service.types.ExecutionError","title":"<code>ExecutionError</code>","text":"<p>             Bases: <code>BaseModel</code></p> <p>Pydantic model for execution errors.</p> Source code in <code>ivcap_service/types.py</code> <pre><code>class ExecutionError(BaseModel):\n    \"\"\"\n    Pydantic model for execution errors.\n    \"\"\"\n\n    jschema: str = Field(\n        default=\"urn:ivcap:schema.service.error.1\",\n        validation_alias=\"$schema\",\n        serialization_alias=\"$schema\",\n    )\n    error: str = Field(description=\"Error message\")\n    type: str = Field(description=\"Error type\")\n    traceback: str | None = Field(default=None, description=\"traceback\")\n\n    model_config = {\n        \"populate_by_name\": True,\n    }\n</code></pre>"},{"location":"api/utilities/","title":"Utilities","text":"<p>Helper functions and utilities for logging and common operations.</p>"},{"location":"api/utilities/#logger","title":"Logger","text":""},{"location":"api/utilities/#ivcap_service.logger.getLogger","title":"<code>getLogger(name)</code>","text":"Source code in <code>ivcap_service/logger.py</code> <pre><code>def getLogger(name: str) -&gt; logging.Logger:\n    return logging.getLogger(name)\n</code></pre>"},{"location":"api/utilities/#logging-initialization","title":"Logging Initialization","text":""},{"location":"api/utilities/#ivcap_service.logger.logging_init","title":"<code>logging_init(cfg_path=None)</code>","text":"Source code in <code>ivcap_service/logger.py</code> <pre><code>def logging_init(cfg_path: str | None = None):\n    if not cfg_path:\n        script_dir = os.path.dirname(__file__)\n        cfg_path = os.path.join(script_dir, \"logging.json\")\n\n    with open(cfg_path) as file:\n        config = json.load(file)\n        set_service_log_config(config)\n</code></pre>"},{"location":"api/utilities/#general-utilities","title":"General Utilities","text":"Copy Markdown"},{"location":"api/utilities/#ivcap_service.utils","title":"<code>utils</code>","text":""},{"location":"api/utilities/#ivcap_service.utils.get_input_type","title":"<code>get_input_type(func)</code>","text":"<p>Gets the input type of a function.</p> <p>Parameters:</p> Name Type Description Default <code>func</code> <code>Callable</code> <p>The function to get the input type for.</p> required <p>Returns:</p> Type Description <code>tuple[type[BaseModel] | None, dict[str, Any]]</code> <p>A tuple containing: - The first function parameter which is a derived class of a pydantic BaseModel, or None if no such parameter exists. - A dictionary of all additional parameters, where the key is the parameter name and the value is the type.</p> Source code in <code>ivcap_service/utils.py</code> <pre><code>def get_input_type(\n    func: Callable,\n) -&gt; tuple[type[BaseModel] | None, dict[str, Any]]:\n    \"\"\"Gets the input type of a function.\n\n    Args:\n        func: The function to get the input type for.\n\n    Returns:\n        A tuple containing:\n        - The first function parameter which is a derived class of a pydantic BaseModel, or None if no such parameter exists.\n        - A dictionary of all additional parameters, where the key is the parameter name and the value is the type.\n    \"\"\"\n    signature = inspect.signature(func)\n    type_hints: dict[str, Any] = get_type_hints(func)\n\n    # Get the Pydantic model class\n    pydantic_model_class = None\n    pydantic_param_name = None\n    for param_name, param in signature.parameters.items():\n        if (\n            hasattr(param.annotation, \"__mro__\")\n            and BaseModel in param.annotation.__mro__\n        ):\n            pydantic_model_class = param.annotation\n            pydantic_param_name = param_name\n            break\n\n    # Get all additional parameters\n    additional_params = {}\n    for param_name, param in signature.parameters.items():\n        if param_name != pydantic_param_name:\n            param_type = type_hints.get(param_name, Any)\n            additional_params[param_name] = param_type\n\n    return pydantic_model_class, additional_params\n</code></pre>"},{"location":"api/utilities/#ivcap_service.utils.get_function_return_type","title":"<code>get_function_return_type(func)</code>","text":"<p>Extracts the return type from a function.</p> Source code in <code>ivcap_service/utils.py</code> <pre><code>def get_function_return_type(func: Callable) -&gt; Any | None:\n    \"\"\"Extracts the return type from a function.\"\"\"\n    type_hints: dict[str, Any] = get_type_hints(func)\n    # param_types = {k: v for k, v in type_hints.items() if k != 'return'}\n    return_type = type_hints.get(\"return\")\n    # return param_types, return_type\n    return return_type\n</code></pre>"},{"location":"api/utilities/#ivcap_service.utils.file_to_json","title":"<code>file_to_json(file_path)</code>","text":"<p>Reads a file, attempts to parse it as JSON, and returns the content.</p> <p>Parameters:</p> Name Type Description Default <code>file_path</code> <code>str</code> <p>The path to the file to read.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A dictionary containing the parsed JSON content.</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If the file does not exist.</p> <code>ValueError</code> <p>If the file is not a valid JSON file.</p> <code>IOError</code> <p>If there is an error reading the file.</p> Source code in <code>ivcap_service/utils.py</code> <pre><code>def file_to_json(file_path: str) -&gt; dict[str, Any]:\n    \"\"\"\n    Reads a file, attempts to parse it as JSON, and returns the content.\n\n    Args:\n        file_path: The path to the file to read.\n\n    Returns:\n        A dictionary containing the parsed JSON content.\n\n    Raises:\n        FileNotFoundError: If the file does not exist.\n        ValueError: If the file is not a valid JSON file.\n        IOError: If there is an error reading the file.\n    \"\"\"\n    if not os.path.exists(file_path):\n        raise FileNotFoundError(f\"File not found: {file_path}\")\n\n    try:\n        with open(file_path) as f:\n            return json.load(f)\n    except FileNotFoundError as e:\n        raise FileNotFoundError(f\"Error finding file: {file_path}: {e}\")\n    except json.JSONDecodeError as e:\n        raise ValueError(f\"Error decoding JSON from file: {file_path}: {e}\")\n    except Exception as e:\n        raise OSError(f\"Error reading file: {file_path}: {e}\")\n</code></pre>"},{"location":"api/utilities/#ivcap_service.utils.clean_description","title":"<code>clean_description(text)</code>","text":"<p>Cleans a string by removing unnecessary whitespace and newlines, while preserving double newlines.</p> Source code in <code>ivcap_service/utils.py</code> <pre><code>def clean_description(text: str) -&gt; str:\n    \"\"\"\n    Cleans a string by removing unnecessary whitespace and newlines, while preserving double newlines.\n    \"\"\"\n    text = text.replace(\"\\n\\n\", \"TEMP_NEWLINE\")\n    text = text.replace(\"\\n\", \" \")\n    text = \" \".join(text.split())\n    text = text.replace(\"TEMP_NEWLINE\", \"\\n\")\n    return text\n</code></pre>"},{"location":"community/conduct/","title":"Code of Conduct","text":""},{"location":"community/conduct/#our-commitment","title":"Our Commitment","text":"<p>We are committed to providing a welcoming and inspiring community for all. We expect all community members to treat each other with respect and dignity.</p>"},{"location":"community/conduct/#our-standards","title":"Our Standards","text":"<p>Examples of behavior that contributes to creating a positive environment include:</p> <ul> <li>Being respectful and constructive in discussions</li> <li>Welcoming newcomers and helping them get started</li> <li>Focusing on what is best for the community</li> <li>Showing empathy towards other community members</li> <li>Using welcoming and inclusive language</li> </ul> <p>Examples of unacceptable behavior include:</p> <ul> <li>Harassment, discrimination, or bullying</li> <li>Offensive comments related to gender, sexual orientation, disability, physical appearance, body size, race, or religion</li> <li>Unwelcome sexual attention or advances</li> <li>Trolling, insulting/derogatory comments, and personal or political attacks</li> <li>Public or private harassment</li> <li>Publishing others' private information without permission</li> <li>Other conduct which could reasonably be considered inappropriate</li> </ul>"},{"location":"community/conduct/#enforcement","title":"Enforcement","text":"<p>Project maintainers are responsible for clarifying the standards of acceptable behavior and are expected to take appropriate and fair corrective action in response to any instances of unacceptable behavior.</p> <p>Instances of abusive, harassing, or otherwise unacceptable behavior may be reported by contacting the project team. All complaints will be reviewed and investigated.</p>"},{"location":"community/conduct/#attribution","title":"Attribution","text":"<p>This Code of Conduct is adapted from the Contributor Covenant, version 2.0, available at https://www.contributor-covenant.org/version/2/0/code_of_conduct.html.</p>"},{"location":"community/contributing/","title":"Contributing","text":"<p>We welcome contributions to the IVCAP Service SDK!</p>"},{"location":"community/contributing/#getting-started","title":"Getting Started","text":""},{"location":"community/contributing/#1-fork-and-clone","title":"1. Fork and Clone","text":"<pre><code>git clone https://github.com/YOUR_USERNAME/ivcap-service-sdk-python.git\ncd ivcap-service-sdk-python\n</code></pre>"},{"location":"community/contributing/#2-set-up-development-environment","title":"2. Set Up Development Environment","text":"<pre><code>poetry install --with dev\n</code></pre>"},{"location":"community/contributing/#3-create-a-branch","title":"3. Create a Branch","text":"<pre><code>git checkout -b feature/my-feature\n</code></pre>"},{"location":"community/contributing/#development-workflow","title":"Development Workflow","text":""},{"location":"community/contributing/#running-tests","title":"Running Tests","text":"<pre><code>poetry run pytest\npytest --cov\n</code></pre>"},{"location":"community/contributing/#code-quality","title":"Code Quality","text":"<pre><code># Format code\npoetry run ruff format .\n\n# Lint\npoetry run ruff check . --fix\n\n# Type checking\npoetry run pyright\n</code></pre>"},{"location":"community/contributing/#building-documentation","title":"Building Documentation","text":"<pre><code>cd docs\npip install -r requirements-docs.txt\nmkdocs serve\n</code></pre> <p>Visit <code>http://localhost:8000</code> to preview the docs.</p>"},{"location":"community/contributing/#making-changes","title":"Making Changes","text":""},{"location":"community/contributing/#code-changes","title":"Code Changes","text":"<ol> <li>Make your changes in a feature branch</li> <li>Add tests for new functionality</li> <li>Ensure all tests pass: <code>poetry run pytest</code></li> <li>Update documentation as needed</li> </ol>"},{"location":"community/contributing/#documentation-changes","title":"Documentation Changes","text":"<ol> <li>Edit files in <code>docs/docs/</code></li> <li>Test locally: <code>mkdocs serve</code></li> <li>Check formatting and links</li> <li>Submit pull request</li> </ol>"},{"location":"community/contributing/#commit-messages","title":"Commit Messages","text":"<p>Use clear, descriptive commit messages:</p> <pre><code>\u2728 feat: add new feature\n\ud83d\udc1b fix: resolve bug\n\ud83d\udcdd docs: update documentation\n\ud83e\uddea test: add unit tests\n\ud83d\udd28 chore: refactor code\n</code></pre>"},{"location":"community/contributing/#pull-request-process","title":"Pull Request Process","text":"<ol> <li>Update documentation</li> <li>Add/update tests</li> <li>Ensure CI passes</li> <li>Create clear PR description</li> <li>Link related issues</li> </ol>"},{"location":"community/contributing/#code-style","title":"Code Style","text":"<p>We follow Python PEP 8 with these tools:</p> <ul> <li>Ruff \u2014 Linting and formatting</li> <li>Pyright \u2014 Type checking</li> <li>Pytest \u2014 Testing</li> </ul>"},{"location":"community/contributing/#style-guide","title":"Style Guide","text":"<pre><code># Docstrings follow Google style\ndef process_job(req: Request, ctx: JobContext) -&gt; Result:\n    \"\"\"\n    Process a job.\n\n    Args:\n        req: Request to process\n        ctx: Job context\n\n    Returns:\n        Processing result\n\n    Raises:\n        ValueError: If validation fails\n    \"\"\"\n    pass\n\n# Type hints required\ndef add(a: int, b: int) -&gt; int:\n    return a + b\n\n# Use meaningful variable names\nresult = process(data)  # Good\nx = process(y)          # Bad\n</code></pre>"},{"location":"community/contributing/#testing","title":"Testing","text":"<p>Write tests for new features:</p> <pre><code># tests/test_feature.py\nimport pytest\nfrom src.feature import my_function\n\ndef test_my_function_basic():\n    result = my_function(5)\n    assert result == 10\n\ndef test_my_function_error():\n    with pytest.raises(ValueError):\n        my_function(-1)\n</code></pre> <p>Run tests:</p> <pre><code>poetry run pytest tests/\npoetry run pytest tests/test_feature.py -v\n</code></pre>"},{"location":"community/contributing/#documentation-guidelines","title":"Documentation Guidelines","text":"<ol> <li>Be clear \u2014 Write for developers new to the SDK</li> <li>Include examples \u2014 Every concept should have a code example</li> <li>Link between pages \u2014 Use relative links to other docs</li> <li>Update TOC \u2014 If adding new pages, update mkdocs.yml</li> </ol>"},{"location":"community/contributing/#reporting-issues","title":"Reporting Issues","text":""},{"location":"community/contributing/#bug-reports","title":"Bug Reports","text":"<p>Include: - Minimal reproducible example - Expected vs actual behavior - Python version and environment - Traceback (if applicable)</p>"},{"location":"community/contributing/#feature-requests","title":"Feature Requests","text":"<p>Include: - Use case and motivation - Proposed API/syntax - Any alternatives considered</p>"},{"location":"community/contributing/#questions","title":"Questions?","text":"<ul> <li>GitHub Issues \u2014 For bugs and feature requests</li> <li>Discussions \u2014 For questions and ideas</li> <li>Email \u2014 For security issues</li> </ul>"},{"location":"community/contributing/#license","title":"License","text":"<p>By contributing, you agree that your contributions will be licensed under the same LICENSE as the project.</p> <p>Thank you for contributing to IVCAP! \ud83d\ude80</p>"},{"location":"examples/advanced-patterns/","title":"Advanced Patterns","text":"<p>Advanced techniques for building sophisticated IVCAP services.</p>"},{"location":"examples/advanced-patterns/#service-composition-pipeline","title":"Service Composition Pipeline","text":"<p>Build complex workflows by composing multiple services:</p> <pre><code>import time\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    validate_svc = ivcap.get_service_by_name(\"validate-service\")\n    transform_svc = ivcap.get_service_by_name(\"transform-service\")\n    enrich_svc   = ivcap.get_service_by_name(\"enrich-service\")\n\n    with ctxt.report.step(\"validate\", msg=\"Validating\") as s:\n        ValidateReq = validate_svc.request_model\n        validated_job = validate_svc.request_job(ValidateReq(data=req.data), timeout=60)\n        s.finished()\n\n    with ctxt.report.step(\"transform\", msg=\"Transforming\") as s:\n        TransformReq = transform_svc.request_model\n        transformed_job = transform_svc.request_job(\n            TransformReq(data=validated_job.result), timeout=120\n        )\n        s.finished()\n\n    with ctxt.report.step(\"enrich\", msg=\"Enriching\") as s:\n        EnrichReq = enrich_svc.request_model\n        enriched_job = enrich_svc.request_job(\n            EnrichReq(data=transformed_job.result), timeout=120\n        )\n        s.finished()\n\n    return Result(result=enriched_job.result)\n</code></pre>"},{"location":"examples/advanced-patterns/#parallel-fan-out","title":"Parallel Fan-Out","text":"<p>Dispatch multiple downstream jobs in parallel, then collect results:</p> <pre><code>import time\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n    results = {}\n\n    processor = ivcap.get_service_by_name(\"item-processor\")\n    ProcessorReq = processor.request_model\n\n    # Dispatch all jobs without blocking (timeout=0)\n    jobs = {\n        item_id: processor.request_job(ProcessorReq(data=item), timeout=0)\n        for item_id, item in enumerate(req.items)\n    }\n\n    # Poll until all finish\n    completed = 0\n    with ctxt.report.step(\"parallel_processing\", msg=\"Waiting for parallel jobs\") as step:\n        while completed &lt; len(jobs):\n            for item_id, job in jobs.items():\n                if item_id not in results and job.finished:\n                    results[item_id] = job.result\n                    completed += 1\n                elif not job.finished:\n                    job.refresh()\n\n            if completed &lt; len(jobs):\n                step.info(event={\"completed\": completed, \"total\": len(jobs)})\n                time.sleep(3)\n\n        step.finished(msg=f\"Completed {len(results)} jobs\")\n\n    return Result(results=results)\n</code></pre>"},{"location":"examples/advanced-patterns/#streaming-large-files","title":"Streaming Large Files","text":"<p>Handle large artifacts efficiently using <code>as_stream()</code> or <code>as_local_file()</code>:</p> <pre><code>import io\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n    chunk_size = 1024 * 1024  # 1 MB\n\n    artifact = ivcap.get_artifact(req.input_urn)\n\n    with ctxt.report.step(\"process\", msg=\"Streaming and processing\") as step:\n        processed_chunks = []\n        bytes_processed = 0\n\n        for chunk in artifact.as_stream():\n            processed = process_chunk(chunk)\n            processed_chunks.append(processed)\n            bytes_processed += len(chunk)\n\n            if bytes_processed % (10 * 1024 * 1024) == 0:  # Every 10 MB\n                step.info(event={\"bytes_processed\": bytes_processed})\n\n        step.finished(msg=f\"Processed {bytes_processed} bytes\")\n\n    with ctxt.report.step(\"upload\", msg=\"Uploading result\") as step:\n        final_data = b\"\".join(processed_chunks)\n        result_artifact = ivcap.upload_artifact(\n            name=\"processed-output.bin\",\n            io_stream=io.BytesIO(final_data),\n            content_type=\"application/octet-stream\",\n            content_size=len(final_data),\n        )\n        step.finished(msg=f\"Uploaded {result_artifact.id}\")\n\n    return Result(artifact_urn=result_artifact.id)\n</code></pre> <p>For very large files where you want minimal memory footprint, download directly to disk:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    artifact = ivcap.get_artifact(req.input_urn)\n\n    with ctxt.report.step(\"process\", msg=\"Processing large file\") as step:\n        # as_local_file() streams the artifact to a temp file \u2014 no memory spike\n        with artifact.as_local_file() as path:\n            result = process_large_file(path)  # path is a pathlib.Path\n        step.finished()\n\n    return Result(result=result)\n</code></pre>"},{"location":"examples/advanced-patterns/#retry-with-exponential-backoff","title":"Retry with Exponential Backoff","text":"<p>Robust error handling with exponential backoff:</p> <pre><code>import time\nimport random\nfrom ivcap_client.exception import IvcapApiError\n\ndef retry_with_backoff(func, max_retries=3, base_wait=1):\n    for attempt in range(max_retries):\n        try:\n            return func()\n        except Exception as e:\n            if attempt == max_retries - 1:\n                raise\n            wait_time = base_wait * (2 ** attempt) + random.uniform(0, 1)\n            logger.warning(f\"Attempt {attempt+1} failed, retrying in {wait_time:.1f}s: {e}\")\n            time.sleep(wait_time)\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    downstream = ivcap.get_service_by_name(\"flaky-service\")\n    DownstreamReq = downstream.request_model\n\n    with ctxt.report.step(\"call_service\", msg=\"Calling service with retry\") as step:\n        job = retry_with_backoff(\n            lambda: downstream.request_job(DownstreamReq(data=req.data), timeout=60)\n        )\n        step.finished()\n\n    return Result(result=job.result)\n</code></pre>"},{"location":"examples/advanced-patterns/#dynamic-service-discovery","title":"Dynamic Service Discovery","text":"<p>Find and invoke services at runtime based on request parameters:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    # Look up service by name from the request\n    svc = ivcap.get_service_by_name(req.processor_service_name)\n    SvcReq = svc.request_model\n\n    # Inspect available parameters (useful for debugging)\n    for name, param in svc.parameters.items():\n        logger.debug(f\"  {name}: {param.type}, optional={param.is_optional}\")\n\n    with ctxt.report.step(\"process\", msg=f\"Using {req.processor_service_name}\") as step:\n        job = svc.request_job(SvcReq(data=req.data), timeout=300)\n        step.finished()\n\n    return Result(result=job.result)\n</code></pre>"},{"location":"examples/advanced-patterns/#conditional-processing","title":"Conditional Processing","text":"<p>Branch to different services based on input type or size:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    if req.type == \"small\":\n        svc_name = \"fast-processor\"\n    elif req.type == \"large\":\n        svc_name = \"powerful-processor\"\n    else:\n        svc_name = \"default-processor\"\n\n    svc = ivcap.get_service_by_name(svc_name)\n    SvcReq = svc.request_model\n\n    with ctxt.report.step(\"process\", msg=f\"Using {svc_name}\") as step:\n        job = svc.request_job(SvcReq(data=req.data), timeout=300)\n        step.finished()\n\n    return Result(result=job.result)\n</code></pre>"},{"location":"examples/advanced-patterns/#caching-strategy","title":"Caching Strategy","text":"<p>Implement in-process caching to avoid redundant computation across jobs:</p> <pre><code>from functools import lru_cache\nimport hashlib\n\n@lru_cache(maxsize=100)\ndef get_cached_model(model_id: str):\n    \"\"\"Load model once per process \u2014 reused across jobs.\"\"\"\n    return load_model(model_id)\n\nresult_cache: dict = {}\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    # Check in-memory cache\n    input_hash = hashlib.md5(req.data).hexdigest()\n    if input_hash in result_cache:\n        logger.info(\"Cache hit!\")\n        return Result(result=result_cache[input_hash])\n\n    # Process\n    with ctxt.report.step(\"process\", msg=\"Processing\") as step:\n        model = get_cached_model(req.model_id)\n        result = model.predict(req.data)\n        step.finished()\n\n    # Cache for future jobs\n    result_cache[input_hash] = result\n\n    return Result(result=result)\n</code></pre>"},{"location":"examples/advanced-patterns/#see-also","title":"See Also","text":"<ul> <li>Service Composition Guide</li> <li>Artifacts Guide</li> <li>Job Processing Guide</li> <li>Best Practices</li> </ul> Copy Markdown"},{"location":"examples/batch-service/","title":"Batch Service Example","text":"<p>A complete example of a production-ready IVCAP batch service.</p>"},{"location":"examples/batch-service/#overview","title":"Overview","text":"<p>This service processes batches of text items with validation, transformation, and error handling.</p>"},{"location":"examples/batch-service/#code","title":"Code","text":"<pre><code>from pydantic import BaseModel, Field, field_validator\nfrom ivcap_service import (\n    Service, ServiceContact, ServiceLicense,\n    JobContext, start_batch_service,\n    getLogger, logging_init, with_schema,\n)\nimport time\n\n# Initialize logging\nlogging_init()\nlogger = getLogger(\"batch_service\")\n\n# Define service\nservice = Service(\n    name=\"Batch Text Processor\",\n    contact=ServiceContact(name=\"Developer\", email=\"dev@example.com\"),\n    license=ServiceLicense(name=\"MIT\", url=\"https://opensource.org/license/MIT\"),\n)\n\n# Helper item type (no schema needed \u2014 embedded in BatchRequest)\nclass Item(BaseModel):\n    id: str\n    text: str = Field(min_length=1, max_length=1000)\n\n# Request schema\n@with_schema(\"urn:sd:schema:batch-text-processor.request.1\")\nclass BatchRequest(BaseModel):\n    items: list[Item] = Field(min_items=1)\n    uppercase: bool = Field(default=False)\n\n# Helper result item type\nclass ProcessedItem(BaseModel):\n    id: str\n    original: str\n    processed: str\n    word_count: int\n\n# Result schema\n@with_schema(\"urn:sd:schema:batch-text-processor.1\")\nclass BatchResult(BaseModel):\n    items: list[ProcessedItem]\n    total_processed: int\n    total_time_seconds: float\n\n# Job processor\ndef process_job(req: BatchRequest, ctx: JobContext) -&gt; BatchResult:\n    \"\"\"Process a batch of text items.\"\"\"\n    logger.info(f\"Processing batch with {len(req.items)} items\")\n    start_time = time.time()\n\n    results = []\n\n    # Step 1: Validate inputs\n    with ctx.report.step(\"validate\", msg=\"Validating inputs\") as step:\n        for item in req.items:\n            if not item.text.strip():\n                raise ValueError(f\"Item {item.id}: empty text\")\n        step.finished(msg=f\"Validated {len(req.items)} items\")\n\n    # Step 2: Process items\n    with ctx.report.step(\"process\", msg=\"Processing items\") as step:\n        for i, item in enumerate(req.items):\n            # Transform\n            processed_text = item.text.upper() if req.uppercase else item.text\n            word_count = len(processed_text.split())\n\n            results.append(ProcessedItem(\n                id=item.id,\n                original=item.text,\n                processed=processed_text,\n                word_count=word_count\n            ))\n\n            # Report progress\n            if (i + 1) % 10 == 0:\n                step.info(event={\n                    \"processed\": i + 1,\n                    \"total\": len(req.items),\n                    \"percentage\": ((i + 1) / len(req.items)) * 100\n                })\n\n        step.finished(msg=f\"Processed {len(results)} items\")\n\n    # Step 3: Summary\n    duration = time.time() - start_time\n\n    with ctx.report.step(\"summary\") as step:\n        step.info(event={\n            \"total_items\": len(results),\n            \"duration_seconds\": duration,\n            \"items_per_second\": len(results) / duration\n        })\n        step.finished()\n\n    return BatchResult(\n        items=results,\n        total_processed=len(results),\n        total_time_seconds=duration\n    )\n\nif __name__ == \"__main__\":\n    start_batch_service(service, process_job)\n</code></pre>"},{"location":"examples/batch-service/#testing","title":"Testing","text":"<p>Create <code>test_request.json</code>:</p> <pre><code>{\n    \"items\": [\n        {\"id\": \"1\", \"text\": \"Hello world\"},\n        {\"id\": \"2\", \"text\": \"IVCAP service\"},\n        {\"id\": \"3\", \"text\": \"Batch processing\"}\n    ],\n    \"uppercase\": true\n}\n</code></pre> <p>Test locally:</p> <pre><code>python batch_service.py --test-file test_request.json\n</code></pre> <p>Expected output:</p> <pre><code>Processing batch with 3 items\nValidated 3 items\nProcessed 3 items\n...\n</code></pre>"},{"location":"examples/batch-service/#key-features","title":"Key Features","text":"<ol> <li>Validation \u2014 Pydantic validates request automatically</li> <li>Progress Tracking \u2014 Reports progress with step events</li> <li>Error Handling \u2014 Explicit error reporting</li> <li>Metrics \u2014 Tracks processing time and statistics</li> <li>Logging \u2014 Structured logging for debugging</li> </ol>"},{"location":"examples/batch-service/#see-also","title":"See Also","text":"<ul> <li>Job Processing Guide</li> <li>Artifacts Guide</li> <li>Best Practices</li> </ul> Copy Markdown"},{"location":"getting-started/first-service/","title":"Your First Service","text":"<p>In this guide, we'll build a complete, production-ready service that processes images.</p>"},{"location":"getting-started/first-service/#project-setup","title":"Project Setup","text":"<p>Create a new directory for your service:</p> <pre><code>mkdir image-processor\ncd image-processor\n</code></pre> <p>Create a <code>pyproject.toml</code>:</p> <pre><code>[tool.poetry]\nname = \"image-processor\"\nversion = \"0.1.0\"\ndescription = \"IVCAP service for processing images\"\n\n[tool.poetry.dependencies]\npython = \"^3.10\"\nivcap_service = \"^0.6.0\"\npillow = \"^10.0.0\"\npydantic = \"^2.0\"\n\n[build-system]\nrequires = [\"poetry-core\"]\nbuild-backend = \"poetry.core.masonry.api\"\n</code></pre> <p>Install dependencies:</p> <pre><code>poetry install\n</code></pre>"},{"location":"getting-started/first-service/#step-1-define-your-service","title":"Step 1: Define Your Service","text":"<p>Create <code>image_processor.py</code>:</p> <pre><code>from pydantic import BaseModel, Field\nfrom ivcap_service import (\n    Service, ServiceContact, ServiceLicense,\n    JobContext, start_batch_service, getLogger, logging_init, with_schema,\n)\n\n# Initialize logging\nlogging_init()\nlogger = getLogger(\"image_processor\")\n\n# Define your service\nservice = Service(\n    name=\"Image Processor\",\n    contact=ServiceContact(name=\"Your Name\", email=\"you@example.com\"),\n    license=ServiceLicense(name=\"MIT\", url=\"https://opensource.org/license/MIT\"),\n)\n\n# Define request schema\n@with_schema(\"urn:sd:schema:image-processor.request.1\")\nclass ImageRequest(BaseModel):\n    \"\"\"Request to process an image.\"\"\"\n    image_artifact_id: str = Field(description=\"ID of artifact containing image\")\n    width: int = Field(default=800, description=\"Target width in pixels\")\n    height: int = Field(default=600, description=\"Target height in pixels\")\n\n# Define result schema\n@with_schema(\"urn:sd:schema:image-processor.1\")\nclass ImageResult(BaseModel):\n    \"\"\"Result after processing.\"\"\"\n    processed_image_id: str = Field(description=\"ID of processed image artifact\")\n    original_size: dict = Field(description=\"Original image dimensions\")\n    final_size: dict = Field(description=\"Final image dimensions\")\n\n# Process a job\ndef process_job(request: ImageRequest, context: JobContext) -&gt; ImageResult:\n    \"\"\"Process an image by resizing.\"\"\"\n    logger.info(f\"Processing job {context.job_id}\")\n\n    ivcap = context.ivcap  # property \u2014 no parentheses; always available in platform jobs\n\n    # Step 1: Download the image\n    with context.report.step(\"download\", msg=\"Downloading image\") as step:\n        logger.info(f\"Downloading artifact {request.image_artifact_id}\")\n        artifact = ivcap.get_artifact(request.image_artifact_id)\n        # as_local_file() streams to a temp file and auto-deletes on exit\n        with artifact.as_local_file() as path:\n            image_bytes = path.read_bytes()\n        step.finished(msg=f\"Downloaded {len(image_bytes)} bytes\")\n\n    # Step 2: Process the image\n    with context.report.step(\"process\", msg=\"Resizing image\") as step:\n        from PIL import Image\n        import io\n\n        # Load image\n        original_image = Image.open(io.BytesIO(image_bytes))\n        original_size = original_image.size\n        logger.info(f\"Original size: {original_size}\")\n\n        # Resize\n        final_image = original_image.resize((request.width, request.height))\n        final_size = final_image.size\n        logger.info(f\"Resized to: {final_size}\")\n\n        # Convert to bytes\n        output_buffer = io.BytesIO()\n        final_image.save(output_buffer, format=\"JPEG\", quality=85)\n        processed_data = output_buffer.getvalue()\n\n        step.info(event={\n            \"original_dimensions\": original_size,\n            \"final_dimensions\": final_size,\n            \"bytes\": len(processed_data)\n        })\n        step.finished(msg=\"Image resized\")\n\n    # Step 3: Upload the result\n    with context.report.step(\"upload\", msg=\"Uploading result\") as step:\n        result_artifact = ivcap.upload_artifact(\n            name=\"resized-image.jpg\",\n            io_stream=io.BytesIO(processed_data),\n            content_type=\"image/jpeg\",\n            content_size=len(processed_data),\n        )\n        logger.info(f\"Uploaded result: {result_artifact.id}\")\n        step.finished(msg=f\"Uploaded: {result_artifact.id}\")\n\n    return ImageResult(\n        processed_image_id=result_artifact.id,\n        original_size={\"width\": original_size[0], \"height\": original_size[1]},\n        final_size={\"width\": final_size[0], \"height\": final_size[1]}\n    )\n\nif __name__ == \"__main__\":\n    start_batch_service(service, process_job)\n</code></pre>"},{"location":"getting-started/first-service/#step-2-create-test-data","title":"Step 2: Create Test Data","text":"<p>Create <code>test_request.json</code>:</p> <pre><code>{\n    \"image_artifact_id\": \"urn:ivcap:artifact:test\",\n    \"width\": 400,\n    \"height\": 300\n}\n</code></pre>"},{"location":"getting-started/first-service/#step-3-test-locally","title":"Step 3: Test Locally","text":"<p>Print the service metadata:</p> <pre><code>poetry run python image_processor.py --print-service-description\n</code></pre> <p>You should see:</p> <pre><code>{\n  \"name\": \"Image Processor\",\n  \"description\": \"Resize and optimize images\",\n  ...\n}\n</code></pre>"},{"location":"getting-started/first-service/#step-4-dockerize-your-service","title":"Step 4: Dockerize Your Service","text":"<p>Create a <code>Dockerfile</code>:</p> <pre><code>FROM python:3.11-slim\n\nWORKDIR /app\n\n# Install dependencies\nCOPY pyproject.toml poetry.lock* ./\nRUN pip install poetry &amp;&amp; \\\n    poetry install --no-dev --no-root\n\n# Copy service code\nCOPY image_processor.py .\n\n# Run the service\nENTRYPOINT [\"poetry\", \"run\", \"python\", \"image_processor.py\"]\n</code></pre> <p>Build the image:</p> <pre><code>docker build -t image-processor:latest .\n</code></pre>"},{"location":"getting-started/first-service/#step-5-register-with-ivcap","title":"Step 5: Register with IVCAP","text":"<pre><code># Get the service definition\ndocker run image-processor:latest --print-service-description &gt; service.json\n\n# Register with IVCAP\nivcap service register service.json\n</code></pre>"},{"location":"getting-started/first-service/#next-steps","title":"Next Steps","text":"<ul> <li>Job Processing Guide \u2014 Learn advanced patterns</li> <li>Artifact Management \u2014 Work with files</li> <li>Error Handling \u2014 Robust services</li> <li>Deployment \u2014 Production setup</li> <li>Best Practices \u2014 Pro tips</li> </ul>"},{"location":"getting-started/first-service/#common-extensions","title":"Common Extensions","text":""},{"location":"getting-started/first-service/#add-more-parameters","title":"Add More Parameters","text":"<pre><code>@with_schema(\"urn:sd:schema:image-processor.request.1\")\nclass ImageRequest(BaseModel):\n    image_artifact_id: str\n    width: int = 800\n    height: int = 600\n    quality: int = Field(default=85, ge=1, le=100)\n    format: str = Field(default=\"JPEG\", description=\"Output format: JPEG, PNG, etc\")\n</code></pre>"},{"location":"getting-started/first-service/#parallel-processing","title":"Parallel Processing","text":"<pre><code>import concurrent.futures\n\ndef process_batch(requests, context):\n    with concurrent.futures.ThreadPoolExecutor() as executor:\n        futures = [executor.submit(process_single, req) for req in requests]\n        results = [f.result() for f in futures]\n    return results\n</code></pre>"},{"location":"getting-started/first-service/#caching","title":"Caching","text":"<pre><code>from functools import lru_cache\n\n@lru_cache(maxsize=100)\ndef get_model(model_path):\n    # Load model once, reuse for multiple jobs\n    return load_model(model_path)\n</code></pre>"},{"location":"getting-started/first-service/#troubleshooting","title":"Troubleshooting","text":""},{"location":"getting-started/first-service/#no-module-named-pil","title":"\"No module named 'PIL'\"","text":"<p>The Pillow package provides PIL:</p> <pre><code>poetry add pillow\n</code></pre>"},{"location":"getting-started/first-service/#testing-artifact-downloadupload-locally","title":"Testing artifact download/upload locally","text":"<p>When testing with <code>--test-file</code>, the <code>ctxt.ivcap</code> client connects to a real IVCAP deployment (via <code>IVCAP_BASE_URL</code>).  If you are testing offline, the <code>as_local_file()</code> call will fail because there is no platform to download from. For local integration testing, use <code>LocalIVCAP</code> from <code>ivcap_client</code>:</p> <pre><code>from ivcap_client import IVCAP\nivcap = IVCAP.local(base_dir=\"./test-artifacts\")\nartifact = ivcap.get_artifact(\"file:///path/to/local-image.jpg\")\n</code></pre>"},{"location":"getting-started/first-service/#docker-build-fails","title":"Docker build fails","text":"<p>Ensure <code>poetry.lock</code> exists or let pip install handle dependencies:</p> <pre><code>RUN pip install poetry &amp;&amp; \\\n    poetry config virtualenvs.create false &amp;&amp; \\\n    poetry install --no-dev\n</code></pre>"},{"location":"getting-started/first-service/#see-also","title":"See Also","text":"<ul> <li>API Reference \u2014 All classes and functions</li> <li>Examples \u2014 More complete examples</li> </ul> Copy Markdown"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10 or higher</li> <li>pip or poetry package manager</li> </ul>"},{"location":"getting-started/installation/#from-pypi","title":"From PyPI","text":"<p>The easiest way to install the IVCAP Service SDK:</p> <pre><code>pip install ivcap_service\n</code></pre> <p>Or with poetry:</p> <pre><code>poetry add ivcap_service\n</code></pre>"},{"location":"getting-started/installation/#from-source","title":"From Source","text":"<p>For development or to use the latest unreleased features:</p> <pre><code>git clone https://github.com/ivcap-works/ivcap-service-sdk-python.git\ncd ivcap-service-sdk-python\npoetry install\n</code></pre>"},{"location":"getting-started/installation/#verify-installation","title":"Verify Installation","text":"<p>Test your installation:</p> <pre><code>python -c \"import ivcap_service; print(ivcap_service.__version__)\"\n</code></pre> <p>You should see the version number printed.</p>"},{"location":"getting-started/installation/#optional-development-dependencies","title":"Optional: Development Dependencies","text":"<p>If you plan to contribute or build documentation locally:</p> <pre><code># Development tools\npoetry install --with dev\n\n# Documentation tools\npip install -r docs/requirements-docs.txt\n</code></pre>"},{"location":"getting-started/installation/#next-steps","title":"Next Steps","text":"<ul> <li>Read the Quick Start guide</li> <li>Build your First Service</li> <li>Check out Examples</li> </ul>"},{"location":"getting-started/installation/#troubleshooting","title":"Troubleshooting","text":""},{"location":"getting-started/installation/#importerror-no-module-named-ivcap_service","title":"ImportError: No module named 'ivcap_service'","text":"<p>Make sure you've installed the package: <pre><code>pip install ivcap_service\n</code></pre></p> <p>If using a virtual environment, ensure it's activated.</p>"},{"location":"getting-started/installation/#python-version-error","title":"Python Version Error","text":"<p>Check your Python version: <pre><code>python --version\n</code></pre></p> <p>The SDK requires Python 3.10+. If you have multiple Python versions installed, use: <pre><code>python3.10 -m pip install ivcap_service\n</code></pre></p>"},{"location":"getting-started/installation/#permission-denied","title":"Permission Denied","text":"<p>If you get permission errors during installation, use: <pre><code>pip install --user ivcap_service\n</code></pre></p>"},{"location":"getting-started/installation/#getting-help","title":"Getting Help","text":"<ul> <li>Check Common Issues</li> <li>Open an issue</li> <li>See Contributing</li> </ul> Copy Markdown"},{"location":"getting-started/quick-start/","title":"Quick Start","text":"<p>Get a working IVCAP service running in 5 minutes.</p>"},{"location":"getting-started/quick-start/#1-install-the-sdk","title":"1. Install the SDK","text":"<pre><code>pip install ivcap_service\n</code></pre>"},{"location":"getting-started/quick-start/#2-create-your-service","title":"2. Create Your Service","text":"<p>Save this as <code>my_service.py</code>:</p> <pre><code>from pydantic import BaseModel, Field\nfrom ivcap_service import (\n    Service, ServiceContact, ServiceLicense,\n    JobContext, start_batch_service, getLogger, logging_init, with_schema,\n)\n\n# Initialize logging\nlogging_init()\nlogger = getLogger(\"my_service\")\n\n# Define your service\nservice = Service(\n    name=\"My First Service\",\n    contact=ServiceContact(name=\"Your Name\", email=\"you@example.com\"),\n    license=ServiceLicense(name=\"MIT\", url=\"https://opensource.org/license/MIT\"),\n)\n\n# Define request schema\n@with_schema(\"urn:sd:schema:my_service.request.1\")\nclass TextRequest(BaseModel):\n    text: str = Field(description=\"Text to convert to uppercase\")\n\n# Define result schema\n@with_schema(\"urn:sd:schema:my_service.result.1\")\nclass TextResult(BaseModel):\n    uppercase_text: str = Field(description=\"Converted text\")\n\n# Process a job\ndef process_job(request: TextRequest, context: JobContext) -&gt; TextResult:\n    \"\"\"Convert input text to uppercase.\"\"\"\n    logger.info(f\"Processing job {context.job_id}\")\n\n    with context.report.step(\"converting\", msg=\"Converting text...\") as step:\n        result = request.text.upper()\n        step.finished(msg=f\"Converted: {request.text}\")\n\n    return TextResult(uppercase_text=result)\n\n# Run the service\nif __name__ == \"__main__\":\n    start_batch_service(service, process_job)\n</code></pre>"},{"location":"getting-started/quick-start/#3-test-your-service","title":"3. Test Your Service","text":"<p>Test it locally with a JSON file. Create <code>test_request.json</code>:</p> <pre><code>{\n    \"$schema\": \"urn:sd:schema:my_service.request.1\",\n    \"text\": \"hello world\"\n}\n</code></pre> <p>Run the test:</p> <pre><code>python my_service.py --test-file test_request.json\n</code></pre> <p>You should see: <pre><code>Processing job test-job-id\n...\nConverted: hello world\nuppercase_text: HELLO WORLD\n</code></pre></p>"},{"location":"getting-started/quick-start/#4-print-service-metadata","title":"4. Print Service Metadata","text":"<p>View the service definition that will be registered with IVCAP:</p> <pre><code>python my_service.py --print-service-description\n</code></pre>"},{"location":"getting-started/quick-start/#5-run-the-service","title":"5. Run the Service","text":"<p>To run the service (waiting for jobs from IVCAP):</p> <pre><code>python my_service.py\n</code></pre> <p>The service will start and wait for jobs. In production, this runs in a container orchestrated by IVCAP.</p>"},{"location":"getting-started/quick-start/#next-steps","title":"Next Steps","text":"<ul> <li>Your First Service \u2014 Build a more complete example</li> <li>Job Processing Guide \u2014 Learn advanced patterns</li> <li>Examples \u2014 See production-ready code</li> <li>API Reference \u2014 Complete class/function documentation</li> </ul>"},{"location":"getting-started/quick-start/#common-patterns","title":"Common Patterns","text":""},{"location":"getting-started/quick-start/#accessing-job-metadata","title":"Accessing Job Metadata","text":"<pre><code>def process_job(request: TextRequest, context: JobContext) -&gt; TextResult:\n    print(f\"Job ID: {context.job_id}\")\n    print(f\"Request ID: {context.request_id}\")\n    # More in JobContext API...\n</code></pre>"},{"location":"getting-started/quick-start/#reporting-progress","title":"Reporting Progress","text":"<pre><code>with context.report.step(\"step1\") as step:\n    # Do work...\n    step.finished(msg=\"Completed step 1\")\n\nwith context.report.step(\"step2\") as step:\n    # Do more work...\n    step.info(event={\"key\": \"value\"})  # Send info event\n    step.finished()\n</code></pre>"},{"location":"getting-started/quick-start/#error-handling","title":"Error Handling","text":"<pre><code>def process_job(request: TextRequest, context: JobContext) -&gt; TextResult:\n    try:\n        # Process...\n        pass\n    except Exception as e:\n        context.report.step(\"error\").error(e)\n        raise\n</code></pre>"},{"location":"getting-started/quick-start/#troubleshooting","title":"Troubleshooting","text":""},{"location":"getting-started/quick-start/#no-module-named-ivcap_service","title":"\"No module named 'ivcap_service'\"","text":"<p>See Installation</p>"},{"location":"getting-started/quick-start/#service-wont-run","title":"Service won't run","text":"<p>Check that you have the <code>process_job</code> function signature correct: - Takes <code>(request_type, JobContext)</code> - Returns <code>result_type</code></p>"},{"location":"getting-started/quick-start/#test-file-not-found","title":"Test file not found","text":"<p>Make sure the test file exists and is in the correct location.</p> <p>For more help, see Best Practices.</p> Copy Markdown"},{"location":"guides/artifacts/","title":"Working with Artifacts","text":"<p>This guide covers downloading, uploading, and managing artifacts in your IVCAP services using the <code>ctxt.ivcap</code> client available inside every job.</p>"},{"location":"guides/artifacts/#overview","title":"Overview","text":"<p>An Artifact is any binary or structured data blob consumed or produced by a job: an image, a CSV file, a NetCDF dataset, a trained model checkpoint, etc.</p> <p>Your services commonly:</p> <ul> <li>Download input artifacts passed in as request parameters</li> <li>Upload results as new artifacts</li> <li>Organize artifacts in collections</li> <li>Attach typed metadata (aspects) to artifacts</li> </ul> <p><code>ctxt.ivcap</code> is always available inside a job worker function. It is pre-configured with the platform-injected credentials \u2014 no explicit authentication is needed.</p>"},{"location":"guides/artifacts/#accessing-the-ivcap-client","title":"Accessing the IVCAP Client","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap  # property \u2014 no parentheses\n    ...\n</code></pre>"},{"location":"guides/artifacts/#downloading-artifacts","title":"Downloading Artifacts","text":"<p>Three methods are available, each suited to a different use case:</p> Method Best for <code>artifact.as_local_file()</code> Recommended \u2014 saves to disk with minimal code <code>artifact.open()</code> Loading small artifacts entirely into memory <code>artifact.as_stream()</code> Custom chunk processing or piping into external APIs"},{"location":"guides/artifacts/#download-to-a-local-file-recommended","title":"Download to a local file (recommended)","text":"<p><code>as_local_file()</code> handles all streaming internals for you and supports two patterns:</p> <p>Temporary file (auto-deleted when the <code>with</code> block exits):</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    artifact = ivcap.get_artifact(req.input_artifact_urn)\n\n    with artifact.as_local_file() as path:\n        logger.info(f\"Downloaded to: {path}\")\n        data = path.read_bytes()\n        result = process(data)\n    # temp file deleted here\n\n    return Result(result=result)\n</code></pre> <p>Explicit path (file is kept after the <code>with</code> block):</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    artifact = ivcap.get_artifact(req.input_artifact_urn)\n    path = artifact.as_local_file(\"/tmp/input.csv\")\n\n    logger.info(f\"Saved to: {path}\")\n    result = process_csv(path)\n\n    return Result(result=result)\n</code></pre> <p>Explicit path with a suggested name (useful when you want the SDK to choose the directory but control the filename):</p> <pre><code>with artifact.as_local_file(\"my_input.jpg\") as path:\n    img = Image.open(path)\n</code></pre>"},{"location":"guides/artifacts/#open-as-an-in-memory-file-object","title":"Open as an in-memory file object","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    artifact = ivcap.get_artifact(req.input_artifact_urn)\n\n    with artifact.open() as f:\n        data = f.read()           # bytes\n        text = data.decode(\"utf-8\")\n\n    return Result(result=process(text))\n</code></pre> <p>Warning</p> <p><code>open()</code> loads the entire blob into memory.  For large artifacts (hundreds of MB+) prefer <code>as_local_file()</code> or <code>as_stream()</code>.</p>"},{"location":"guides/artifacts/#stream-in-chunks-advanced","title":"Stream in chunks (advanced)","text":"<p>Use <code>as_stream()</code> when you need low-level control: progress reporting, piping bytes into a third-party API, or incremental processing.</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    artifact = ivcap.get_artifact(req.input_artifact_urn)\n\n    total = 0\n    with open(\"/tmp/output.bin\", \"wb\") as f:\n        for chunk in artifact.as_stream():\n            f.write(chunk)\n            total += len(chunk)\n\n    logger.info(f\"Downloaded {total} bytes\")\n    return Result(result=process_file(\"/tmp/output.bin\"))\n</code></pre>"},{"location":"guides/artifacts/#uploading-artifacts","title":"Uploading Artifacts","text":""},{"location":"guides/artifacts/#upload-from-an-in-memory-stream","title":"Upload from an in-memory stream","text":"<pre><code>import io\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    result_data = process(req.data)\n\n    artifact = ivcap.upload_artifact(\n        name=\"my-result.json\",\n        io_stream=io.BytesIO(result_data),\n        content_type=\"application/json\",\n        content_size=len(result_data),\n    )\n\n    logger.info(f\"Uploaded artifact: {artifact.id}\")\n    return Result(artifact_urn=artifact.id)\n</code></pre>"},{"location":"guides/artifacts/#upload-a-local-file","title":"Upload a local file","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    # Write output to a temp file\n    output_path = \"/tmp/result.csv\"\n    with open(output_path, \"w\") as f:\n        f.write(generate_csv(req.data))\n\n    # Upload the file\n    artifact = ivcap.upload_artifact(\n        name=\"result.csv\",\n        file_path=output_path,\n        content_type=\"text/csv\",\n    )\n\n    logger.info(f\"Uploaded: {artifact.id}\")\n    return Result(artifact_urn=artifact.id)\n</code></pre>"},{"location":"guides/artifacts/#upload-parameters","title":"Upload parameters","text":"Parameter Type Description <code>name</code> <code>str</code> Human-readable name for the artifact <code>file_path</code> <code>str</code> Local path to upload <code>io_stream</code> <code>IO</code> In-memory stream (requires <code>content_type</code>) <code>content_type</code> <code>str</code> MIME type (auto-detected from <code>file_path</code> extension if omitted) <code>content_size</code> <code>int</code> Size in bytes (-1 = unknown) <code>collection</code> <code>URN</code> Add to a named collection (<code>urn:ivcap:collection:...</code>) <code>policy</code> <code>URN</code> Access policy (<code>urn:ivcap:policy:...</code>)"},{"location":"guides/artifacts/#adding-metadata-aspects-to-artifacts","title":"Adding Metadata (Aspects) to Artifacts","text":"<p>Attach structured domain metadata to any artifact in the Datafabric:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    artifact = ivcap.get_artifact(req.artifact_urn)\n\n    # Attach custom metadata\n    ivcap.add_aspect(\n        entity=artifact.id,\n        aspect={\n            \"$schema\": \"urn:my-service:schema:processing-result.1\",\n            \"processed_by\": \"my-service\",\n            \"processing_time_seconds\": 42.5,\n            \"status\": \"success\",\n            \"tags\": [\"important\", \"verified\"],\n        },\n    )\n\n    return Result(artifact_urn=artifact.id)\n</code></pre>"},{"location":"guides/artifacts/#common-patterns","title":"Common Patterns","text":""},{"location":"guides/artifacts/#download-process-upload-pipeline","title":"Download \u2192 Process \u2192 Upload pipeline","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    with ctxt.report.step(\"load\", msg=\"Downloading input\") as s:\n        artifact = ivcap.get_artifact(req.input_urn)\n        with artifact.as_local_file() as path:\n            data = path.read_bytes()\n        s.finished(msg=f\"Downloaded {len(data)} bytes\")\n\n    with ctxt.report.step(\"transform\", msg=\"Transforming\") as s:\n        transformed = transform(data)\n        s.finished(msg=f\"Transformed {len(data)} bytes\")\n\n    with ctxt.report.step(\"validate\", msg=\"Validating\") as s:\n        validate(transformed)\n        s.finished()\n\n    with ctxt.report.step(\"save\", msg=\"Uploading result\") as s:\n        import io\n        output_artifact = ivcap.upload_artifact(\n            name=\"result.bin\",\n            io_stream=io.BytesIO(transformed),\n            content_type=\"application/octet-stream\",\n            content_size=len(transformed),\n        )\n        s.finished(msg=f\"Uploaded {output_artifact.id}\")\n\n    return Result(output_urn=output_artifact.id)\n</code></pre>"},{"location":"guides/artifacts/#batch-upload-to-a-collection","title":"Batch upload to a collection","text":"<pre><code>import io\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n    collection_id = \"urn:ivcap:collection:batch-results\"\n    artifact_urns = []\n\n    with ctxt.report.step(\"batch_processing\", msg=\"Processing items\") as step:\n        for i, item in enumerate(req.items):\n            processed = process(item)\n            artifact = ivcap.upload_artifact(\n                name=f\"item-{i}.bin\",\n                io_stream=io.BytesIO(processed),\n                content_type=\"application/octet-stream\",\n                content_size=len(processed),\n                collection=collection_id,\n            )\n            artifact_urns.append(artifact.id)\n\n            if i % 100 == 0:\n                step.info(event={\"processed\": i, \"total\": len(req.items)})\n\n        step.finished(msg=f\"Uploaded {len(artifact_urns)} artifacts\")\n\n    return Result(artifact_urns=artifact_urns)\n</code></pre>"},{"location":"guides/artifacts/#streaming-large-files","title":"Streaming large files","text":"<p>For very large files, stream directly to disk using <code>as_local_file()</code> or process chunks with <code>as_stream()</code>:</p> <pre><code>import io\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    artifact = ivcap.get_artifact(req.input_urn)\n\n    with ctxt.report.step(\"process\", msg=\"Streaming large file\") as step:\n        processed_chunks = []\n        bytes_read = 0\n\n        for chunk in artifact.as_stream():\n            processed = process_chunk(chunk)\n            processed_chunks.append(processed)\n            bytes_read += len(chunk)\n\n            if bytes_read % (10 * 1024 * 1024) == 0:  # every 10 MB\n                step.info(event={\"bytes_read\": bytes_read})\n\n        step.finished(msg=f\"Processed {bytes_read} bytes\")\n\n    with ctxt.report.step(\"upload\", msg=\"Uploading result\") as step:\n        final_data = b\"\".join(processed_chunks)\n        result_artifact = ivcap.upload_artifact(\n            name=\"processed-output.bin\",\n            io_stream=io.BytesIO(final_data),\n            content_type=\"application/octet-stream\",\n            content_size=len(final_data),\n        )\n        step.finished(msg=f\"Uploaded {result_artifact.id}\")\n\n    return Result(artifact_urn=result_artifact.id)\n</code></pre>"},{"location":"guides/artifacts/#error-handling","title":"Error Handling","text":"<pre><code>from ivcap_client.exception import ResourceNotFound, IvcapApiError\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    with ctxt.report.step(\"download\", msg=\"Downloading\") as step:\n        try:\n            artifact = ivcap.get_artifact(req.input_urn)\n            with artifact.as_local_file() as path:\n                data = path.read_bytes()\n            step.finished(msg=f\"Downloaded {len(data)} bytes\")\n        except ResourceNotFound:\n            logger.warning(f\"Artifact not found: {req.input_urn}, using default data\")\n            step.finished(msg=\"Using default data\")\n            data = DEFAULT_DATA\n        except IvcapApiError as e:\n            logger.error(f\"Platform API error [{e.status_code}]: {e}\")\n            raise\n\n    with ctxt.report.step(\"upload\", msg=\"Uploading\") as step:\n        try:\n            import io\n            result_artifact = ivcap.upload_artifact(\n                name=\"result.bin\",\n                io_stream=io.BytesIO(process(data)),\n                content_type=\"application/octet-stream\",\n            )\n            step.finished(msg=f\"Uploaded {result_artifact.id}\")\n        except IvcapApiError as e:\n            logger.error(f\"Upload failed [{e.status_code}]: {e}\")\n            raise\n\n    return Result(artifact_urn=result_artifact.id)\n</code></pre>"},{"location":"guides/artifacts/#see-also","title":"See Also","text":"<ul> <li>Job Processing \u2014 Core patterns</li> <li>Service Composition \u2014 Calling other services</li> <li>Deployment \u2014 Production setup</li> <li>ivcap-client SDK \u2014 Artifacts \u2014 Full client-side artifact reference</li> </ul> Copy Markdown"},{"location":"guides/best-practices/","title":"Best Practices Guide","text":"<p>Build production-quality IVCAP services with these patterns.</p>"},{"location":"guides/best-practices/#code-organization","title":"Code Organization","text":""},{"location":"guides/best-practices/#structure-your-service","title":"Structure Your Service","text":"<pre><code>my-service/\n\u251c\u2500\u2500 src/\n\u2502   \u251c\u2500\u2500 __init__.py\n\u2502   \u251c\u2500\u2500 service.py          # Service definition &amp; entry point\n\u2502   \u251c\u2500\u2500 processor.py        # Job processing logic\n\u2502   \u251c\u2500\u2500 models/\n\u2502   \u2502   \u251c\u2500\u2500 request.py      # Request schemas\n\u2502   \u2502   \u2514\u2500\u2500 result.py       # Result schemas\n\u2502   \u2514\u2500\u2500 utils/\n\u2502       \u251c\u2500\u2500 __init__.py\n\u2502       \u251c\u2500\u2500 artifacts.py    # Artifact utilities\n\u2502       \u251c\u2500\u2500 logger.py       # Logging setup\n\u2502       \u2514\u2500\u2500 validators.py   # Custom validators\n\u251c\u2500\u2500 tests/\n\u2502   \u251c\u2500\u2500 test_processor.py\n\u2502   \u2514\u2500\u2500 test_models.py\n\u251c\u2500\u2500 pyproject.toml\n\u251c\u2500\u2500 Dockerfile\n\u2514\u2500\u2500 README.md\n</code></pre>"},{"location":"guides/best-practices/#separate-concerns","title":"Separate Concerns","text":"<pre><code># service.py\nfrom src.processor import process_job\nfrom src.models import RequestSchema, ResultSchema\n\nservice = Service(name=\"My Service\", ...)\n\nif __name__ == \"__main__\":\n    start_batch_service(service, process_job)\n\n# processor.py\nfrom src.models import RequestSchema, ResultSchema\nfrom src.utils import validate_input, process_data\n\ndef process_job(req: RequestSchema, ctx: JobContext) -&gt; ResultSchema:\n    validate_input(req)\n    result = process_data(req.data)\n    return ResultSchema(result=result)\n</code></pre>"},{"location":"guides/best-practices/#testing","title":"Testing","text":""},{"location":"guides/best-practices/#unit-tests","title":"Unit Tests","text":"<pre><code># tests/test_processor.py\nfrom src.processor import process_job\nfrom src.models import RequestSchema, ResultSchema\nimport pytest\n\ndef test_process_job_basic():\n    req = RequestSchema(data=\"test\")\n    # Mock the context\n    result = process_job(req, mock_context)\n    assert isinstance(result, ResultSchema)\n    assert result.result == \"TEST\"\n\ndef test_process_job_invalid_input():\n    with pytest.raises(ValueError):\n        req = RequestSchema(data=\"\")\n        result = process_job(req, mock_context)\n</code></pre>"},{"location":"guides/best-practices/#integration-tests","title":"Integration Tests","text":"<pre><code># tests/test_integration.py\ndef test_service_with_local_file():\n    \"\"\"Test service with actual test data.\"\"\"\n    import json\n    with open(\"tests/test_request.json\") as f:\n        req_data = json.load(f)\n\n    # Create context\n    ctx = create_test_context()\n\n    # Process\n    result = process_job_from_dict(req_data, ctx)\n\n    # Verify\n    assert result[\"status\"] == \"success\"\n</code></pre>"},{"location":"guides/best-practices/#performance-optimization","title":"Performance Optimization","text":""},{"location":"guides/best-practices/#caching","title":"Caching","text":"<pre><code>from functools import lru_cache\n\n@lru_cache(maxsize=100)\ndef load_model(model_path: str):\n    \"\"\"Load ML model once, reuse.\"\"\"\n    return torch.load(model_path)\n\ndef process_job(req: Request, ctx: JobContext) -&gt; Result:\n    model = load_model(\"/path/to/model\")\n    prediction = model.predict(req.data)\n    return Result(prediction=prediction)\n</code></pre>"},{"location":"guides/best-practices/#batch-processing","title":"Batch Processing","text":"<pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    items = req.items\n    batch_size = 32\n    results = []\n\n    with ctx.report.step(\"processing\") as step:\n        for i in range(0, len(items), batch_size):\n            batch = items[i:i+batch_size]\n            batch_results = process_batch(batch)\n            results.extend(batch_results)\n\n    return Result(results=results)\n</code></pre>"},{"location":"guides/best-practices/#memory-management","title":"Memory Management","text":"<pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Process in chunks instead of loading all\n    results = []\n\n    with ctx.report.step(\"processing\") as step:\n        for chunk in iter_chunks(req.data, chunk_size=10_000):\n            result = process_chunk(chunk)\n            results.append(result)\n            # Chunk is garbage collected automatically\n\n    return Result(results=results)\n</code></pre>"},{"location":"guides/best-practices/#error-handling-best-practices","title":"Error Handling Best Practices","text":"<ol> <li>Fail fast \u2014 Detect invalid input early</li> <li>Log context \u2014 Include relevant information</li> <li>Graceful degradation \u2014 Use fallbacks when possible</li> <li>Report progress \u2014 Even if failing, report where</li> </ol> <pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # 1. Validate early\n    if not validate(req):\n        raise ValueError(\"Invalid input\")\n\n    try:\n        with ctx.report.step(\"processing\") as step:\n            # 2. Include context in logs\n            logger = getLogger(f\"job-{ctx.job_id}\")\n            logger.info(\"Starting processing\")\n\n            result = process(req)\n            step.finished()\n    except Exception as e:\n        # 3. Report where we failed\n        logger.error(f\"Processing failed: {e}\", exc_info=True)\n        raise\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/best-practices/#documentation-best-practices","title":"Documentation Best Practices","text":""},{"location":"guides/best-practices/#docstring-standards","title":"Docstring Standards","text":"<pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    \"\"\"\n    Process a batch of data items.\n\n    This service:\n    - Validates input format\n    - Applies transformation\n    - Returns results\n\n    Args:\n        req: Request containing items to process\n        ctx: Job context for progress and platform access\n\n    Returns:\n        Result with processed items\n\n    Raises:\n        ValueError: If input validation fails\n    \"\"\"\n    # Implementation...\n</code></pre>"},{"location":"guides/best-practices/#service-documentation","title":"Service Documentation","text":"<pre><code>from ivcap_service import Service, ServiceContact, ServiceLicense\n\nservice = Service(\n    name=\"Data Processor\",\n    contact=ServiceContact(name=\"Support Team\", email=\"support@example.com\"),\n    license=ServiceLicense(name=\"MIT\", url=\"https://opensource.org/license/MIT\"),\n)\n</code></pre>"},{"location":"guides/best-practices/#monitoring-best-practices","title":"Monitoring Best Practices","text":""},{"location":"guides/best-practices/#log-at-appropriate-levels","title":"Log at Appropriate Levels","text":"<pre><code>logger.debug(\"Variable x = 5\")  # Development\nlogger.info(\"Job started\")       # Important milestones\nlogger.warning(\"Falling back\")   # Unexpected but recoverable\nlogger.error(\"Processing failed\") # Errors\n</code></pre>"},{"location":"guides/best-practices/#include-context-in-events","title":"Include Context in Events","text":"<pre><code>with ctx.report.step(\"processing\") as step:\n    step.info(event={\n        \"job_id\": ctx.job_id,\n        \"stage\": \"validation\",\n        \"items_count\": len(items),\n        \"timestamp\": datetime.now().isoformat()\n    })\n</code></pre>"},{"location":"guides/best-practices/#configuration-best-practices","title":"Configuration Best Practices","text":""},{"location":"guides/best-practices/#use-environment-variables","title":"Use Environment Variables","text":"<pre><code>import os\n\n# Good: Use environment variables\napi_url = os.getenv(\"API_URL\", \"http://localhost:8000\")\ntimeout = int(os.getenv(\"API_TIMEOUT\", \"30\"))\n\n# Bad: Hardcode values\napi_url = \"http://production.example.com\"\n</code></pre>"},{"location":"guides/best-practices/#validate-configuration","title":"Validate Configuration","text":"<pre><code>def get_config():\n    config = {\n        \"api_url\": os.getenv(\"API_URL\"),\n        \"api_key\": os.getenv(\"API_KEY\"),\n    }\n\n    # Validate required settings\n    if not config[\"api_key\"]:\n        raise ValueError(\"API_KEY environment variable not set\")\n\n    return config\n</code></pre>"},{"location":"guides/best-practices/#security-best-practices","title":"Security Best Practices","text":"<ol> <li>Never hardcode secrets \u2014 Use environment variables or secret manager</li> <li>Validate input \u2014 Use Pydantic validation</li> <li>Sanitize output \u2014 Don't leak sensitive information</li> <li>Use HTTPS \u2014 For all external communication</li> </ol> <pre><code>from ivcap_service import get_secret\n\ndef process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Good: Get from secret manager\n    api_key = get_secret(\"external_api_key\")\n\n    # Good: Validate input with Pydantic\n    # Automatically validates req type\n\n    # Bad: Don't log secrets\n    logger.info(f\"Using API key: {api_key}\")\n\n    # Good: Log masked version\n    logger.info(\"Using API key: ****\")\n</code></pre>"},{"location":"guides/best-practices/#common-pitfalls","title":"Common Pitfalls","text":""},{"location":"guides/best-practices/#dont-store-state-between-jobs","title":"\u274c Don't: Store State Between Jobs","text":"<pre><code># BAD - State persists between jobs\nshared_state = {}\n\ndef process_job(req: Request, ctx: JobContext) -&gt; Result:\n    shared_state[\"count\"] = shared_state.get(\"count\", 0) + 1\n</code></pre>"},{"location":"guides/best-practices/#do-keep-jobs-stateless","title":"\u2705 Do: Keep Jobs Stateless","text":"<pre><code># GOOD - Each job is independent\ndef process_job(req: Request, ctx: JobContext) -&gt; Result:\n    count = len(req.items)\n    return Result(count=count)\n</code></pre>"},{"location":"guides/best-practices/#dont-ignore-exceptions","title":"\u274c Don't: Ignore Exceptions","text":"<pre><code># BAD\ntry:\n    result = process(req)\nexcept:\n    pass  # Silent failure\n</code></pre>"},{"location":"guides/best-practices/#do-handle-and-report-errors","title":"\u2705 Do: Handle and Report Errors","text":"<pre><code># GOOD\ntry:\n    result = process(req)\nexcept Exception as e:\n    logger.error(f\"Processing failed: {e}\")\n    raise\n</code></pre>"},{"location":"guides/best-practices/#see-also","title":"See Also","text":"<ul> <li>Job Processing \u2014 Core patterns</li> <li>Deployment \u2014 Production setup</li> <li>Error Handling \u2014 Robust error handling</li> </ul> Copy Markdown"},{"location":"guides/deployment/","title":"Deployment Guide","text":"<p>Deploy your IVCAP service to production.</p>"},{"location":"guides/deployment/#docker-containerization","title":"Docker Containerization","text":"<p>Create a <code>Dockerfile</code>:</p> <pre><code>FROM python:3.11-slim\n\nWORKDIR /app\n\n# Install dependencies\nCOPY pyproject.toml poetry.lock* ./\nRUN pip install poetry &amp;&amp; \\\n    poetry config virtualenvs.create false &amp;&amp; \\\n    poetry install --no-dev\n\n# Copy service code\nCOPY my_service.py .\n\n# Run the service\nENTRYPOINT [\"python\", \"my_service.py\"]\n</code></pre> <p>Build the image:</p> <pre><code>docker build -t my-service:latest .\n</code></pre>"},{"location":"guides/deployment/#environment-configuration","title":"Environment Configuration","text":""},{"location":"guides/deployment/#local-development","title":"Local Development","text":"<pre><code># Optional: OpenObserve integration\nexport OPENOBSERVE_URL=\"http://localhost:5080\"\nexport OPENOBSERVE_ORG=\"myorg\"\nexport OPENOBSERVE_USERNAME=\"admin@example.com\"\nexport OPENOBSERVE_TOKEN=\"&lt;token&gt;\"\n\n# Run the service\npython my_service.py --print-service-description\n</code></pre>"},{"location":"guides/deployment/#production-environment","title":"Production Environment","text":"<p>Set environment variables in your deployment:</p> <pre><code># Service configuration\nexport IVCAP_URL=\"https://ivcap.example.com\"\n\n# Observability\nexport OPENOBSERVE_URL=\"https://observe.example.com\"\nexport OPENOBSERVE_ORG=\"production\"\nexport OPENOBSERVE_USERNAME=\"service@example.com\"\nexport OPENOBSERVE_TOKEN=\"&lt;production-token&gt;\"\n\n# Custom settings\nexport API_KEY=\"&lt;external-api-key&gt;\"\nexport LOG_LEVEL=\"INFO\"\n</code></pre>"},{"location":"guides/deployment/#service-registration","title":"Service Registration","text":"<p>Get the service definition:</p> <pre><code>docker run my-service:latest --print-service-description &gt; service.json\n</code></pre> <p>Register with IVCAP:</p> <pre><code>ivcap service register service.json\n</code></pre>"},{"location":"guides/deployment/#kubernetes-deployment","title":"Kubernetes Deployment","text":"<p>Create a <code>deployment.yaml</code>:</p> <pre><code>apiVersion: apps/v1\nkind: Deployment\nmetadata:\n  name: my-service\nspec:\n  replicas: 3\n  selector:\n    matchLabels:\n      app: my-service\n  template:\n    metadata:\n      labels:\n        app: my-service\n    spec:\n      containers:\n      - name: my-service\n        image: my-service:latest\n        env:\n        - name: OPENOBSERVE_URL\n          value: \"https://observe.example.com\"\n        - name: OPENOBSERVE_ORG\n          valueFrom:\n            secretKeyRef:\n              name: observability\n              key: org\n        - name: OPENOBSERVE_TOKEN\n          valueFrom:\n            secretKeyRef:\n              name: observability\n              key: token\n        resources:\n          requests:\n            memory: \"256Mi\"\n            cpu: \"250m\"\n          limits:\n            memory: \"512Mi\"\n            cpu: \"500m\"\n</code></pre> <p>Deploy:</p> <pre><code>kubectl apply -f deployment.yaml\n</code></pre>"},{"location":"guides/deployment/#scalability-patterns","title":"Scalability Patterns","text":""},{"location":"guides/deployment/#horizontal-scaling","title":"Horizontal Scaling","text":"<p>The IVCAP platform automatically distributes jobs across service instances. Design your service to be stateless:</p> <pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Don't store state between jobs\n    # Each job runs independently\n    return Result(result=process(req.data))\n</code></pre>"},{"location":"guides/deployment/#resource-requirements","title":"Resource Requirements","text":"<p>Specify resource needs in your service definition. The platform uses this for scheduling:</p> <pre><code>from ivcap_service import ResourceRequirements\n\nservice = Service(\n    name=\"GPU-Intensive Service\",\n    resources=ResourceRequirements(\n        cpu=2,\n        memory=\"4Gi\",\n        gpu=1  # Request GPU\n    )\n)\n</code></pre>"},{"location":"guides/deployment/#monitoring","title":"Monitoring","text":""},{"location":"guides/deployment/#health-checks","title":"Health Checks","text":"<p>Implement health monitoring:</p> <pre><code># Docker health check\nHEALTHCHECK --interval=30s --timeout=10s --start-period=5s --retries=3 \\\n    CMD python -c \"import ivcap_service; print('healthy')\"\n</code></pre>"},{"location":"guides/deployment/#log-aggregation","title":"Log Aggregation","text":"<p>Logs automatically export to OpenObserve. Query them:</p> <pre><code># View service logs\nivcap logs -service my-service -hours 1\n</code></pre>"},{"location":"guides/deployment/#troubleshooting","title":"Troubleshooting","text":""},{"location":"guides/deployment/#container-wont-start","title":"Container Won't Start","text":"<p>Check logs:</p> <pre><code>docker logs my-service\n</code></pre> <p>Ensure service code is correct:</p> <pre><code># Test locally\ndocker run -it my-service:latest --print-service-description\n</code></pre>"},{"location":"guides/deployment/#jobs-failing","title":"Jobs Failing","text":"<p>Check OpenObserve for logs and errors. Common issues:</p> <ol> <li>Missing environment variables</li> <li>Network connectivity to IVCAP</li> <li>Insufficient resources</li> <li>Unhandled exceptions</li> </ol>"},{"location":"guides/deployment/#performance-issues","title":"Performance Issues","text":"<ol> <li>Increase replicas for more parallel processing</li> <li>Optimize code to process faster</li> <li>Profile using OpenObserve metrics</li> <li>Use caching for expensive operations</li> </ol>"},{"location":"guides/deployment/#see-also","title":"See Also","text":"<ul> <li>Best Practices \u2014 Production patterns</li> <li>Observability \u2014 Monitoring setup</li> <li>Error Handling \u2014 Robust error handling</li> </ul> Copy Markdown"},{"location":"guides/error-handling/","title":"Error Handling Guide","text":"<p>Build robust services with comprehensive error handling.</p>"},{"location":"guides/error-handling/#automatic-error-reporting","title":"Automatic Error Reporting","text":"<p>Exceptions are automatically caught and reported:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    # Any exception here is automatically reported to the platform\n    result = process(req)  # If this raises, it's reported as a job failure\n    return Result(result=result)\n</code></pre>"},{"location":"guides/error-handling/#manual-error-reporting","title":"Manual Error Reporting","text":"<p>Explicitly report errors within named steps:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    with ctxt.report.step(\"processing\", msg=\"Processing\") as step:\n        try:\n            result = process(req)\n        except ValueError as e:\n            logger.error(f\"Invalid input: {e}\")\n            raise\n        except Exception as e:\n            logger.error(f\"Unexpected error: {e}\", exc_info=True)\n            raise\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/error-handling/#try-except-patterns","title":"Try-Except Patterns","text":""},{"location":"guides/error-handling/#recover-from-specific-errors","title":"Recover from Specific Errors","text":"<p>Use typed exceptions from <code>ivcap_client.exception</code> for platform-specific errors:</p> <pre><code>from ivcap_client.exception import ResourceNotFound, IvcapApiError\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    with ctxt.report.step(\"download\", msg=\"Downloading artifact\") as step:\n        try:\n            artifact = ivcap.get_artifact(req.artifact_urn)\n            with artifact.as_local_file() as path:\n                data = path.read_bytes()\n            step.finished(msg=f\"Downloaded {len(data)} bytes\")\n        except ResourceNotFound:\n            logger.warning(f\"Artifact not found: {req.artifact_urn}, using default data\")\n            step.finished(msg=\"Using default data\")\n            data = DEFAULT_DATA\n        except IvcapApiError as e:\n            logger.error(f\"Platform API error [{e.status_code}]: {e}\")\n            raise\n\n    return Result(result=process(data))\n</code></pre>"},{"location":"guides/error-handling/#retry-logic","title":"Retry Logic","text":"<pre><code>import time\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n    max_retries = 3\n\n    downstream = ivcap.get_service_by_name(\"my-downstream-service\")\n    DownstreamReq = downstream.request_model\n\n    for attempt in range(max_retries):\n        try:\n            with ctxt.report.step(f\"attempt-{attempt+1}\", msg=f\"Attempt {attempt+1}\") as step:\n                job = downstream.request_job(DownstreamReq(data=req.data), timeout=120)\n                step.finished(msg=\"Succeeded\")\n                return Result(result=job.result)\n        except Exception as e:\n            if attempt &lt; max_retries - 1:\n                wait_time = 2 ** attempt\n                logger.warning(f\"Attempt {attempt+1} failed, retrying in {wait_time}s: {e}\")\n                time.sleep(wait_time)\n            else:\n                logger.error(f\"All {max_retries} attempts failed\")\n                raise\n</code></pre>"},{"location":"guides/error-handling/#graceful-degradation","title":"Graceful Degradation","text":"<p>Try a high-quality service and fall back gracefully if it's unavailable:</p> <pre><code>from ivcap_client.exception import ResourceNotFound, IvcapApiError\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    with ctxt.report.step(\"process\", msg=\"Processing\") as step:\n        try:\n            hq_svc = ivcap.get_service_by_name(\"hq-processor\")\n            HqReq = hq_svc.request_model\n            job = hq_svc.request_job(HqReq(data=req.data), timeout=120)\n            result = job.result\n            step.finished(msg=\"High-quality processing complete\")\n        except (ResourceNotFound, IvcapApiError) as e:\n            logger.warning(f\"HQ service unavailable ({e}), using fallback\")\n            step.info(event={\"fallback\": True})\n\n            fallback_svc = ivcap.get_service_by_name(\"fallback-processor\")\n            FallbackReq = fallback_svc.request_model\n            job = fallback_svc.request_job(FallbackReq(data=req.data), timeout=60)\n            result = job.result\n            step.finished(msg=\"Fallback processing complete\")\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/error-handling/#validation-errors","title":"Validation Errors","text":"<p>Pydantic validates request models automatically:</p> <pre><code>from pydantic import BaseModel, Field, field_validator\nfrom ivcap_service import with_schema\n\n@with_schema(\"urn:sd:schema:my-service.request.1\")\nclass Request(BaseModel):\n    email: str\n    age: int = Field(ge=0, le=150)\n\n    @field_validator(\"email\")\n    @classmethod\n    def validate_email(cls, v):\n        if \"@\" not in v:\n            raise ValueError(\"Invalid email format\")\n        return v\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    # Request is guaranteed to be valid here\n    # Invalid requests cause the job to fail before this function is called\n    return Result(result=f\"Processing {req.email}\")\n</code></pre>"},{"location":"guides/error-handling/#cleanup-on-error","title":"Cleanup on Error","text":"<p>Use <code>as_local_file()</code> as a context manager \u2014 temporary files are cleaned up automatically on context exit.  For other resources, use <code>try/finally</code>:</p> <pre><code>import io\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    artifact = ivcap.get_artifact(req.input_urn)\n\n    # as_local_file() auto-deletes the temp file on exit, even on exception\n    with artifact.as_local_file() as path:\n        result = process_file(path)\n\n    return Result(result=result)\n</code></pre> <p>For non-artifact temporary resources:</p> <pre><code>import tempfile, os\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    tmp_file = None\n    try:\n        with tempfile.NamedTemporaryFile(delete=False, suffix=\".tmp\") as tmp:\n            tmp_file = tmp.name\n            tmp.write(generate_data(req))\n        result = process_file(tmp_file)\n    except Exception as e:\n        logger.error(f\"Processing failed: {e}\", exc_info=True)\n        raise\n    finally:\n        if tmp_file and os.path.exists(tmp_file):\n            os.unlink(tmp_file)\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/error-handling/#partial-success-handling","title":"Partial Success Handling","text":"<p>Continue processing items even when individual ones fail:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    results = []\n    errors = []\n\n    with ctxt.report.step(\"batch_processing\", msg=\"Processing batch\") as step:\n        for i, item in enumerate(req.items):\n            try:\n                result = process_item(item)\n                results.append(result)\n            except Exception as e:\n                errors.append({\n                    \"index\": i,\n                    \"item\": str(item),\n                    \"error\": str(e)\n                })\n                logger.warning(f\"Failed to process item {i}: {e}\")\n\n        step.finished(msg=f\"Processed {len(results)}/{len(req.items)} items successfully\")\n\n    return Result(\n        results=results,\n        errors=errors,\n        success=len(errors) == 0,\n    )\n</code></pre>"},{"location":"guides/error-handling/#custom-error-types","title":"Custom Error Types","text":"<pre><code>class ProcessingError(Exception):\n    \"\"\"Base error for processing failures.\"\"\"\n    pass\n\nclass ValidationError(ProcessingError):\n    \"\"\"Input validation failed.\"\"\"\n    pass\n\nclass ResourceError(ProcessingError):\n    \"\"\"Required resource not available.\"\"\"\n    pass\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    try:\n        if not validate(req):\n            raise ValidationError(\"Invalid input data\")\n\n        return Result(result=process(req))\n\n    except ValidationError as e:\n        logger.error(f\"Validation failed: {e}\")\n        raise\n    except ResourceError as e:\n        logger.error(f\"Resource error: {e}\")\n        raise\n    except Exception as e:\n        logger.error(f\"Unexpected error: {e}\", exc_info=True)\n        raise\n</code></pre>"},{"location":"guides/error-handling/#see-also","title":"See Also","text":"<ul> <li>Job Processing \u2014 Core patterns</li> <li>Observability \u2014 Logging and monitoring</li> <li>Best Practices \u2014 Production patterns</li> <li>ivcap-client SDK \u2014 Error Handling \u2014 Client exception reference</li> </ul> Copy Markdown"},{"location":"guides/job-processing/","title":"Job Processing Guide","text":"<p>This guide covers the fundamentals of processing jobs in IVCAP services.</p>"},{"location":"guides/job-processing/#the-job-processor-function","title":"The Job Processor Function","text":"<p>Every service needs a function that processes jobs:</p> <pre><code>def process_job(request: MyRequest, context: JobContext) -&gt; MyResult:\n    \"\"\"Process a single job.\"\"\"\n    # Your logic here\n    return MyResult(...)\n</code></pre>"},{"location":"guides/job-processing/#function-signature","title":"Function Signature","text":"<p>The function must: 1. Accept exactly 2 parameters:    - Request of your defined type (Pydantic BaseModel)    - <code>JobContext</code> for job operations 2. Return your Result type (Pydantic BaseModel) 3. Can raise exceptions (automatically reported)</p>"},{"location":"guides/job-processing/#request-result-types","title":"Request &amp; Result Types","text":"<p>Use Pydantic models for type safety:</p> <pre><code>from pydantic import BaseModel, Field\nfrom ivcap_service import with_schema\n\n@with_schema(\"urn:sd:schema:my-service.request.1\")\nclass ProcessRequest(BaseModel):\n    \"\"\"Request to process data.\"\"\"\n    input_data: str = Field(description=\"Input to process\")\n    options: dict = Field(default_factory=dict, description=\"Options\")\n\n@with_schema(\"urn:sd:schema:my-service.1\")\nclass ProcessResult(BaseModel):\n    \"\"\"Result after processing.\"\"\"\n    output_data: str = Field(description=\"Processed output\")\n    statistics: dict = Field(default_factory=dict, description=\"Stats\")\n\ndef process_job(req: ProcessRequest, ctx: JobContext) -&gt; ProcessResult:\n    return ProcessResult(\n        output_data=process(req.input_data),\n        statistics={\"items_processed\": 1}\n    )\n</code></pre>"},{"location":"guides/job-processing/#accessing-job-context","title":"Accessing Job Context","text":"<p>The <code>JobContext</code> provides essential information and APIs:</p> <pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Job identifiers\n    print(f\"Job: {ctx.job_id}\")\n    print(f\"Request: {ctx.request_id}\")\n\n    # Progress reporting\n    with ctx.report.step(\"processing\") as step:\n        result = do_work()\n        step.finished()\n\n    # IVCAP platform access (property \u2014 no parentheses; always available in platform jobs)\n    ivcap = ctxt.ivcap\n    artifact = ivcap.get_artifact(artifact_id)\n    with artifact.as_local_file() as path:\n        data = path.read_bytes()\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/job-processing/#progress-reporting","title":"Progress Reporting","text":"<p>Report multi-step workflows using the event system:</p> <pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Step 1: Validate\n    with ctx.report.step(\"validate\", msg=\"Validating input\") as step:\n        if not is_valid(req):\n            raise ValueError(\"Invalid input\")\n        step.finished(msg=\"Validation passed\")\n\n    # Step 2: Process\n    with ctx.report.step(\"process\", msg=\"Processing\") as step:\n        result = process(req)\n        step.info(event={\"progress\": 50})  # Send events\n        step.finished(msg=\"Done\")\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/job-processing/#step-lifecycle","title":"Step Lifecycle","text":"<pre><code>with ctx.report.step(\"step_name\", msg=\"Description\") as step:\n    # Step started automatically\n\n    # Send progress events\n    step.info(event={\"key\": \"value\"})\n\n    # Mark completion\n    step.finished(msg=\"Complete\")\n\n    # Errors automatically reported on exception\n</code></pre>"},{"location":"guides/job-processing/#error-handling","title":"Error Handling","text":"<p>Exceptions are automatically reported:</p> <pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    try:\n        result = process_data(req)\n    except Exception as e:\n        # Automatically reported to context\n        logger.error(f\"Processing failed: {e}\", exc_info=True)\n        raise  # Re-raise to fail the job\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/job-processing/#common-patterns","title":"Common Patterns","text":""},{"location":"guides/job-processing/#multi-step-workflow","title":"Multi-Step Workflow","text":"<pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    with ctx.report.step(\"load\", msg=\"Loading data\") as s:\n        data = load(req.source)\n        s.finished()\n\n    with ctx.report.step(\"transform\", msg=\"Transforming\") as s:\n        transformed = transform(data)\n        s.finished(msg=f\"Transformed {len(data)} items\")\n\n    with ctx.report.step(\"validate\", msg=\"Validating\") as s:\n        validate(transformed)\n        s.finished()\n\n    with ctx.report.step(\"save\", msg=\"Saving\") as s:\n        save(transformed)\n        s.finished()\n\n    return Result(status=\"success\")\n</code></pre>"},{"location":"guides/job-processing/#progress-tracking-with-loops","title":"Progress Tracking with Loops","text":"<pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    items = load_items(req.source)\n    results = []\n\n    with ctx.report.step(\"processing\", msg=\"Processing items\") as step:\n        for i, item in enumerate(items):\n            result = process_item(item)\n            results.append(result)\n\n            # Report progress\n            if i % 100 == 0:\n                step.info(event={\n                    \"processed\": i,\n                    \"total\": len(items),\n                    \"percentage\": (i / len(items)) * 100\n                })\n\n        step.finished(msg=f\"Processed {len(items)} items\")\n\n    return Result(results=results)\n</code></pre>"},{"location":"guides/job-processing/#batch-processing","title":"Batch Processing","text":"<pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    items = load_items(req.source)\n    batch_size = 32\n    results = []\n\n    with ctx.report.step(\"batching\", msg=\"Processing in batches\") as step:\n        for batch_idx in range(0, len(items), batch_size):\n            batch = items[batch_idx:batch_idx + batch_size]\n            batch_results = process_batch(batch)\n            results.extend(batch_results)\n\n            step.info(event={\n                \"batch\": batch_idx // batch_size,\n                \"items_processed\": min(batch_idx + batch_size, len(items))\n            })\n\n        step.finished()\n\n    return Result(results=results)\n</code></pre>"},{"location":"guides/job-processing/#accessing-external-data","title":"Accessing External Data","text":""},{"location":"guides/job-processing/#from-artifacts","title":"From Artifacts","text":"<p>See Artifacts Guide</p>"},{"location":"guides/job-processing/#from-environment-variables","title":"From Environment Variables","text":"<pre><code>import os\n\ndef process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Application settings\n    api_url = os.getenv(\"API_URL\", \"http://localhost:8000\")\n    api_key = os.getenv(\"API_KEY\", \"\")\n\n    # Use in processing\n    result = call_api(api_url, api_key, req.data)\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/job-processing/#validation-and-error-handling","title":"Validation and Error Handling","text":"<p>Pydantic automatically validates request models:</p> <pre><code>from pydantic import BaseModel, Field, field_validator\nfrom ivcap_service import with_schema\n\n@with_schema(\"urn:sd:schema:my-service.request.1\")\nclass Request(BaseModel):\n    email: str = Field(description=\"User email\")\n    count: int = Field(ge=1, le=1000, description=\"Items to process\")\n\n    @field_validator(\"email\")\n    @classmethod\n    def validate_email(cls, v):\n        if \"@\" not in v:\n            raise ValueError(\"Invalid email\")\n        return v\n\ndef process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Request is already validated!\n    # Invalid data causes the job to fail before process_job is called\n    return Result(result=f\"Processing {req.count} items for {req.email}\")\n</code></pre>"},{"location":"guides/job-processing/#performance-considerations","title":"Performance Considerations","text":""},{"location":"guides/job-processing/#caching","title":"Caching","text":"<pre><code>from functools import lru_cache\n\n@lru_cache(maxsize=100)\ndef load_model(model_path: str):\n    \"\"\"Load model once, reuse across jobs.\"\"\"\n    return load_from_disk(model_path)\n\ndef process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Model is loaded once and cached\n    model = load_model(\"/path/to/model\")\n    return Result(result=model.predict(req.input))\n</code></pre>"},{"location":"guides/job-processing/#memory-management","title":"Memory Management","text":"<pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Process in chunks to avoid memory issues\n    results = []\n    for chunk in iter_chunks(req.data, chunk_size=1000):\n        result = process_chunk(chunk)\n        results.extend(result)\n        # Chunk is garbage collected\n\n    return Result(results=results)\n</code></pre>"},{"location":"guides/job-processing/#see-also","title":"See Also","text":"<ul> <li>JobContext API \u2014 Full context documentation</li> <li>Artifacts \u2014 Working with files</li> <li>Error Handling \u2014 Exception handling patterns</li> <li>Examples \u2014 Complete working examples</li> </ul> Copy Markdown"},{"location":"guides/observability/","title":"Observability &amp; Logging Guide","text":"<p>Monitor and debug your services with comprehensive logging and tracing.</p>"},{"location":"guides/observability/#structured-logging","title":"Structured Logging","text":"<p>Initialize logging at startup:</p> <pre><code>from ivcap_service import logging_init, getLogger\n\nlogging_init()\nlogger = getLogger(\"my_service\")\n\nlogger.info(\"Service started\")\nlogger.warning(\"Warning message\")\nlogger.error(\"Error occurred\", exc_info=True)\n</code></pre>"},{"location":"guides/observability/#job-specific-logging","title":"Job-Specific Logging","text":"<pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    # Get job-specific logger\n    logger = getLogger(f\"job-{ctx.job_id}\")\n\n    logger.info(f\"Processing request {ctx.request_id}\")\n\n    with ctx.report.step(\"processing\") as step:\n        logger.debug(\"Starting processing step\")\n        result = process(req)\n        logger.debug(\"Processing complete\")\n        step.finished()\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/observability/#openobserve-integration","title":"OpenObserve Integration","text":"<p>Configure environment variables:</p> <pre><code>export OPENOBSERVE_URL=\"https://observe.example.com\"\nexport OPENOBSERVE_ORG=\"myorg\"\nexport OPENOBSERVE_USERNAME=\"service@example.com\"\nexport OPENOBSERVE_TOKEN=\"&lt;api-token&gt;\"\n</code></pre> <p>Then logs automatically export:</p> <pre><code>from ivcap_service import logging_init, getLogger\n\nlogging_init()\nlogger = getLogger(\"my_service\")\n\n# Logs automatically sent to OpenObserve\nlogger.info(\"Processing started\")\nlogger.error(\"Error occurred\")\n</code></pre>"},{"location":"guides/observability/#progress-events","title":"Progress Events","text":"<p>Use the event system for progress tracking:</p> <pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    with ctx.report.step(\"processing\", msg=\"Processing data\") as step:\n        # Send progress updates\n        for i, item in enumerate(items):\n            process_item(item)\n\n            if i % 100 == 0:\n                step.info(event={\n                    \"progress_percent\": (i / len(items)) * 100,\n                    \"items_processed\": i\n                })\n\n        step.finished(msg=f\"Processed {len(items)} items\")\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/observability/#error-tracking","title":"Error Tracking","text":"<p>Automatically report errors:</p> <pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    with ctx.report.step(\"processing\") as step:\n        try:\n            result = process(req)\n        except Exception as e:\n            # Error automatically reported\n            step.error(e)\n            logger.error(f\"Processing failed: {e}\", exc_info=True)\n            raise\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/observability/#metrics","title":"Metrics","text":"<p>Export metrics to OpenObserve:</p> <pre><code>def process_job(req: Request, ctx: JobContext) -&gt; Result:\n    import time\n\n    start = time.time()\n    result = process(req)\n    duration = time.time() - start\n\n    with ctx.report.step(\"metrics\") as step:\n        step.info(event={\n            \"duration_seconds\": duration,\n            \"items_processed\": len(req.items),\n            \"bytes_processed\": len(req.data)\n        })\n\n    return Result(result=result)\n</code></pre>"},{"location":"guides/observability/#best-practices","title":"Best Practices","text":"<ol> <li>Use job-specific loggers for easier filtering</li> <li>Log at appropriate levels: DEBUG, INFO, WARNING, ERROR</li> <li>Include context in log messages (job ID, step, etc.)</li> <li>Use event system for structured metrics</li> <li>Report errors in both logging and event system</li> </ol>"},{"location":"guides/observability/#see-also","title":"See Also","text":"<ul> <li>Utilities API \u2014 Logging functions</li> <li>Events &amp; Reporting \u2014 Event system</li> <li>Best Practices \u2014 Production patterns</li> </ul> Copy Markdown"},{"location":"guides/overview/","title":"Guides Overview","text":"<p>These guides provide deep dives into specific features and patterns for building IVCAP services.</p>"},{"location":"guides/overview/#getting-started","title":"Getting Started","text":"<p>If you're new to the SDK, start here:</p> <ol> <li>Installation \u2014 Set up the SDK</li> <li>Quick Start \u2014 5-minute introduction</li> <li>Your First Service \u2014 Build a real service</li> </ol>"},{"location":"guides/overview/#core-concepts","title":"Core Concepts","text":""},{"location":"guides/overview/#job-processing","title":"Job Processing","text":"<p>Learn how to define and process jobs: - Request/Result schemas with Pydantic - Accessing job context - Progress tracking and reporting - Common patterns</p> <p>Read this if: You want to understand the fundamentals of building service handlers.</p>"},{"location":"guides/overview/#working-with-artifacts","title":"Working with Artifacts","text":"<p>Upload and download files: - Downloading input artifacts - Uploading results - Handling file streams - Working with collections</p> <p>Read this if: Your service processes or generates files.</p>"},{"location":"guides/overview/#service-composition","title":"Service Composition","text":"<p>Call other IVCAP services: - Service discovery - Dynamic invocation - Parameter passing - Handling results</p> <p>Read this if: You need to compose multiple services together.</p>"},{"location":"guides/overview/#advanced-topics","title":"Advanced Topics","text":""},{"location":"guides/overview/#observability-logging","title":"Observability &amp; Logging","text":"<p>Monitor your services: - Structured logging - OpenObserve integration - Progress events - OpenTelemetry tracing</p> <p>Read this if: You want comprehensive monitoring and debugging.</p>"},{"location":"guides/overview/#error-handling","title":"Error Handling","text":"<p>Build robust services: - Exception handling patterns - Reporting errors - Retry strategies - Graceful degradation</p> <p>Read this if: You need production-grade error handling.</p>"},{"location":"guides/overview/#deployment","title":"Deployment","text":"<p>Deploy to production: - Docker containerization - Environment configuration - Service registration - Scalability patterns</p> <p>Read this if: You're ready to deploy your service.</p>"},{"location":"guides/overview/#best-practices","title":"Best Practices","text":"<p>Pro tips and patterns: - Code organization - Testing strategies - Performance optimization - Common pitfalls</p> <p>Read this if: You want to write production-quality code.</p>"},{"location":"guides/overview/#learning-path","title":"Learning Path","text":"<pre><code>Installation\n    \u2193\nQuick Start\n    \u2193\nYour First Service\n    \u2193\nJob Processing\n    \u251c\u2192 Artifacts (if needed)\n    \u251c\u2192 Service Composition (if needed)\n    \u2514\u2192 Observability (if needed)\n    \u2193\nError Handling\n    \u2193\nDeployment\n    \u2193\nBest Practices\n</code></pre>"},{"location":"guides/overview/#choose-your-own-path","title":"Choose Your Own Path","text":"<ul> <li>I want to process data: Job Processing \u2192 Error Handling</li> <li>I need to work with files: Artifacts</li> <li>I need to call other services: Service Composition</li> <li>I want to monitor my service: Observability</li> <li>I'm ready for production: Deployment \u2192 Best Practices</li> </ul>"},{"location":"guides/overview/#see-also","title":"See Also","text":"<ul> <li>API Reference \u2014 Complete API documentation</li> <li>Examples \u2014 Working code samples</li> <li>Reference \u2014 Configuration and schemas</li> </ul>"},{"location":"guides/service-composition/","title":"Service Composition Guide","text":"<p>Compose multiple IVCAP services to build complex workflows from within your job worker.</p>"},{"location":"guides/service-composition/#overview","title":"Overview","text":"<p>Service composition allows your service to discover and invoke other IVCAP services, enabling powerful patterns like pipelines, fan-out/fan-in, and conditional routing.</p> <p>The <code>ctxt.ivcap</code> client is pre-authenticated \u2014 no credentials are required when running inside a platform job container.</p> <pre><code>flowchart LR\n    A[Job Request] --&gt; B[Your Service]\n    B --&gt; C[Service A]\n    B --&gt; D[Service B]\n    C --&gt; E[Result]\n    D --&gt; E</code></pre>"},{"location":"guides/service-composition/#accessing-the-ivcap-client","title":"Accessing the IVCAP Client","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap  # property \u2014 no parentheses\n    ...\n</code></pre>"},{"location":"guides/service-composition/#service-discovery","title":"Service Discovery","text":""},{"location":"guides/service-composition/#find-a-service-by-name","title":"Find a service by name","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    # Look up a service by its registered name\n    downstream = ivcap.get_service_by_name(\"image-processor\")\n\n    return Result()\n</code></pre>"},{"location":"guides/service-composition/#find-a-service-by-urn","title":"Find a service by URN","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    downstream = ivcap.get_service(\"urn:ivcap:service:my-service-uuid\")\n\n    return Result()\n</code></pre>"},{"location":"guides/service-composition/#list-all-available-services","title":"List all available services","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    for svc in ivcap.list_services(limit=50):\n        logger.info(f\"Available: {svc.name} ({svc.id})\")\n\n    return Result()\n</code></pre>"},{"location":"guides/service-composition/#calling-services","title":"Calling Services","text":""},{"location":"guides/service-composition/#simple-invocation","title":"Simple Invocation","text":"<p>Use <code>service.request_job(req_model, timeout=N)</code> to submit a job and wait for the result.  Pass a Pydantic model (or a <code>dict</code> / <code>IO[str]</code> of JSON) as the request.</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    downstream = ivcap.get_service_by_name(\"my-downstream-service\")\n    DownstreamReq = downstream.request_model\n\n    job = downstream.request_job(\n        DownstreamReq(input_data=req.data, options=req.options),\n        timeout=300,  # wait up to 5 minutes\n    )\n\n    if job.succeeded:\n        logger.info(f\"Downstream result: {job.result}\")\n    else:\n        logger.error(f\"Downstream job failed: {job.status()}\")\n        raise RuntimeError(\"Downstream service failed\")\n\n    return Result(result=job.result)\n</code></pre>"},{"location":"guides/service-composition/#with-progress-reporting","title":"With Progress Reporting","text":"<pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    downstream = ivcap.get_service_by_name(\"my-service\")\n    DownstreamReq = downstream.request_model\n\n    with ctxt.report.step(\"call_service\", msg=\"Calling downstream service\") as step:\n        try:\n            job = downstream.request_job(\n                DownstreamReq(data=req.data),\n                timeout=120,\n            )\n            step.finished(msg=f\"Service completed with status: {job.status()}\")\n        except Exception as e:\n            logger.error(f\"Service call failed: {e}\")\n            raise\n\n    return Result(result=job.result)\n</code></pre>"},{"location":"guides/service-composition/#common-patterns","title":"Common Patterns","text":""},{"location":"guides/service-composition/#pipeline-composition","title":"Pipeline Composition","text":"<p>Chain services sequentially, passing results from one stage to the next:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    validate_svc = ivcap.get_service_by_name(\"validate-service\")\n    transform_svc = ivcap.get_service_by_name(\"transform-service\")\n    enrich_svc = ivcap.get_service_by_name(\"enrich-service\")\n\n    with ctxt.report.step(\"validate\", msg=\"Validating\") as s:\n        ValidateReq = validate_svc.request_model\n        validated_job = validate_svc.request_job(ValidateReq(data=req.data), timeout=60)\n        s.finished(msg=\"Validation complete\")\n\n    with ctxt.report.step(\"transform\", msg=\"Transforming\") as s:\n        TransformReq = transform_svc.request_model\n        transformed_job = transform_svc.request_job(\n            TransformReq(data=validated_job.result), timeout=120\n        )\n        s.finished(msg=\"Transform complete\")\n\n    with ctxt.report.step(\"enrich\", msg=\"Enriching\") as s:\n        EnrichReq = enrich_svc.request_model\n        enriched_job = enrich_svc.request_job(\n            EnrichReq(data=transformed_job.result), timeout=120\n        )\n        s.finished(msg=\"Enrichment complete\")\n\n    return Result(result=enriched_job.result)\n</code></pre>"},{"location":"guides/service-composition/#parallel-fan-out","title":"Parallel Fan-Out","text":"<p>Dispatch multiple downstream jobs in parallel, then aggregate results:</p> <pre><code>import time\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    processor = ivcap.get_service_by_name(\"batch-processor\")\n    ProcessorReq = processor.request_model\n\n    # Dispatch all jobs without waiting (timeout=0 returns immediately)\n    jobs = []\n    for item in req.items:\n        job = processor.request_job(ProcessorReq(data=item), timeout=0)\n        jobs.append(job)\n\n    logger.info(f\"Dispatched {len(jobs)} parallel jobs\")\n\n    # Poll until all jobs finish\n    completed = []\n    with ctxt.report.step(\"wait_for_jobs\", msg=\"Waiting for results\") as step:\n        while len(completed) &lt; len(jobs):\n            for job in jobs:\n                if job not in completed:\n                    job.refresh()\n                    if job.finished:\n                        logger.info(f\"Job {job.id} finished: {job.status()}\")\n                        completed.append(job)\n            if len(completed) &lt; len(jobs):\n                step.info(event={\"completed\": len(completed), \"total\": len(jobs)})\n                time.sleep(5)\n\n        step.finished(msg=f\"All {len(jobs)} jobs finished\")\n\n    results = [j.result for j in jobs if j.succeeded]\n    return Result(results=results, all_succeeded=all(j.succeeded for j in jobs))\n</code></pre>"},{"location":"guides/service-composition/#conditional-routing","title":"Conditional Routing","text":"<p>Route to different services based on input properties:</p> <pre><code>def process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    # Choose service based on data size\n    if req.data_size_mb &lt; 10:\n        svc_name = \"fast-processor\"\n    elif req.data_size_mb &lt; 100:\n        svc_name = \"standard-processor\"\n    else:\n        svc_name = \"heavy-processor\"\n\n    svc = ivcap.get_service_by_name(svc_name)\n    SvcReq = svc.request_model\n\n    with ctxt.report.step(\"process\", msg=f\"Using {svc_name}\") as step:\n        job = svc.request_job(SvcReq(data=req.data), timeout=600)\n        step.finished(msg=f\"Completed via {svc_name}\")\n\n    return Result(result=job.result)\n</code></pre>"},{"location":"guides/service-composition/#graceful-degradation-with-fallback","title":"Graceful Degradation with Fallback","text":"<p>Try a primary service and fall back gracefully on failure:</p> <pre><code>from ivcap_client.exception import ResourceNotFound, IvcapApiError\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    with ctxt.report.step(\"process\", msg=\"Processing\") as step:\n        try:\n            hq_svc = ivcap.get_service_by_name(\"hq-processor\")\n            HqReq = hq_svc.request_model\n            job = hq_svc.request_job(HqReq(data=req.data), timeout=120)\n            step.finished(msg=\"High-quality processing complete\")\n        except (ResourceNotFound, IvcapApiError) as e:\n            logger.warning(f\"HQ service unavailable ({e}), using fallback\")\n            step.info(event={\"fallback\": True})\n\n            fallback_svc = ivcap.get_service_by_name(\"fallback-processor\")\n            FallbackReq = fallback_svc.request_model\n            job = fallback_svc.request_job(FallbackReq(data=req.data), timeout=60)\n            step.finished(msg=\"Fallback processing complete\")\n\n    return Result(result=job.result)\n</code></pre>"},{"location":"guides/service-composition/#map-reduce","title":"Map-Reduce","text":"<p>Submit a large number of jobs and aggregate their outputs:</p> <pre><code>import time\n\ndef process_job(req: Request, ctxt: JobContext) -&gt; Result:\n    ivcap = ctxt.ivcap\n\n    mapper = ivcap.get_service_by_name(\"item-mapper\")\n    reducer = ivcap.get_service_by_name(\"result-reducer\")\n    MapperReq = mapper.request_model\n    ReducerReq = reducer.request_model\n\n    # Map phase: fan out\n    map_jobs = [\n        mapper.request_job(MapperReq(item=item), timeout=0)\n        for item in req.items\n    ]\n\n    # Wait for all map jobs\n    while not all(j.finished for j in map_jobs):\n        for j in map_jobs:\n            if not j.finished:\n                j.refresh()\n        time.sleep(2)\n\n    mapped_results = [j.result for j in map_jobs if j.succeeded]\n\n    # Reduce phase\n    with ctxt.report.step(\"reduce\", msg=\"Reducing results\") as step:\n        reduce_job = reducer.request_job(ReducerReq(items=mapped_results), timeout=120)\n        step.finished()\n\n    return Result(result=reduce_job.result)\n</code></pre>"},{"location":"guides/service-composition/#see-also","title":"See Also","text":"<ul> <li>Job Processing \u2014 Core patterns</li> <li>Artifacts \u2014 Passing artifact URNs between services</li> <li>Error Handling \u2014 Robust service calls</li> <li>ivcap-client SDK \u2014 Services \u2014 Full service-discovery reference</li> <li>ivcap-client SDK \u2014 Jobs \u2014 Job submission and monitoring</li> </ul> Copy Markdown"},{"location":"reference/environment-variables/","title":"Environment Variables Reference","text":"<p>Complete reference of all environment variables used by the IVCAP Service SDK.</p>"},{"location":"reference/environment-variables/#service-configuration","title":"Service Configuration","text":""},{"location":"reference/environment-variables/#ivcap_url","title":"IVCAP_URL","text":"<ul> <li>Type: String</li> <li>Default: Auto-detected from environment</li> <li>Description: Base URL for the IVCAP platform</li> <li>Example: <code>https://ivcap.example.com</code></li> </ul>"},{"location":"reference/environment-variables/#ivcap_api_key","title":"IVCAP_API_KEY","text":"<ul> <li>Type: String</li> <li>Description: API key for IVCAP authentication</li> <li>Example: <code>sk_prod_...</code></li> </ul>"},{"location":"reference/environment-variables/#openobserve-integration","title":"OpenObserve Integration","text":""},{"location":"reference/environment-variables/#openobserve_url","title":"OPENOBSERVE_URL","text":"<ul> <li>Type: String</li> <li>Description: Base URL for OpenObserve instance</li> <li>Example: <code>https://observe.example.com</code></li> </ul>"},{"location":"reference/environment-variables/#openobserve_org","title":"OPENOBSERVE_ORG","text":"<ul> <li>Type: String</li> <li>Description: OpenObserve organization name</li> <li>Example: <code>production</code></li> </ul>"},{"location":"reference/environment-variables/#openobserve_username","title":"OPENOBSERVE_USERNAME","text":"<ul> <li>Type: String</li> <li>Description: OpenObserve username for authentication</li> <li>Example: <code>service@example.com</code></li> </ul>"},{"location":"reference/environment-variables/#openobserve_token","title":"OPENOBSERVE_TOKEN","text":"<ul> <li>Type: String</li> <li>Description: OpenObserve API token</li> <li>Example: <code>zo_prod_...</code></li> </ul>"},{"location":"reference/environment-variables/#openobserve_enable_logs","title":"OPENOBSERVE_ENABLE_LOGS","text":"<ul> <li>Type: Boolean</li> <li>Default: <code>true</code></li> <li>Description: Enable log export to OpenObserve</li> <li>Values: <code>true</code>, <code>false</code>, <code>1</code>, <code>0</code></li> </ul>"},{"location":"reference/environment-variables/#openobserve_enable_metrics","title":"OPENOBSERVE_ENABLE_METRICS","text":"<ul> <li>Type: Boolean</li> <li>Default: <code>true</code></li> <li>Description: Enable metrics export to OpenObserve</li> <li>Values: <code>true</code>, <code>false</code>, <code>1</code>, <code>0</code></li> </ul>"},{"location":"reference/environment-variables/#openobserve_metrics_interval","title":"OPENOBSERVE_METRICS_INTERVAL","text":"<ul> <li>Type: Integer</li> <li>Default: <code>60</code></li> <li>Description: Metrics collection interval in seconds</li> <li>Example: <code>30</code></li> </ul>"},{"location":"reference/environment-variables/#opentelemetry-configuration","title":"OpenTelemetry Configuration","text":""},{"location":"reference/environment-variables/#otel_exporter_otlp_endpoint","title":"OTEL_EXPORTER_OTLP_ENDPOINT","text":"<ul> <li>Type: String</li> <li>Description: Custom OTLP endpoint for metrics and traces</li> <li>Example: <code>http://otel-collector:4318</code></li> </ul>"},{"location":"reference/environment-variables/#otel_exporter_otlp_headers","title":"OTEL_EXPORTER_OTLP_HEADERS","text":"<ul> <li>Type: String</li> <li>Description: Custom headers for OTLP exports</li> <li>Example: <code>Authorization=Bearer &lt;token&gt;,x-scope=service</code></li> </ul>"},{"location":"reference/environment-variables/#openobserve_use_unified_otlp_endpoint","title":"OPENOBSERVE_USE_UNIFIED_OTLP_ENDPOINT","text":"<ul> <li>Type: Boolean</li> <li>Default: <code>false</code></li> <li>Description: Use OpenObserve unified OTLP endpoint</li> <li>Values: <code>true</code>, <code>false</code></li> </ul>"},{"location":"reference/environment-variables/#logging-configuration","title":"Logging Configuration","text":""},{"location":"reference/environment-variables/#ivcap_log_level","title":"IVCAP_LOG_LEVEL","text":"<ul> <li>Type: String</li> <li>Default: <code>INFO</code></li> <li>Description: Global log level</li> <li>Values: <code>DEBUG</code>, <code>INFO</code>, <code>WARNING</code>, <code>ERROR</code>, <code>CRITICAL</code></li> <li>Example: <code>DEBUG</code></li> </ul>"},{"location":"reference/environment-variables/#ivcap_log_format","title":"IVCAP_LOG_FORMAT","text":"<ul> <li>Type: String</li> <li>Description: Log format specification</li> <li>Example: <code>%(asctime)s - %(name)s - %(levelname)s - %(message)s</code></li> </ul>"},{"location":"reference/environment-variables/#custom-application-variables","title":"Custom Application Variables","text":"<p>You can use any environment variables in your service:</p> <pre><code>import os\n\ndef process_job(req: Request, ctx: JobContext) -&gt; Result:\n    api_url = os.getenv(\"API_URL\", \"http://localhost:8000\")\n    api_key = os.getenv(\"API_KEY\")\n    timeout = int(os.getenv(\"API_TIMEOUT\", \"30\"))\n\n    return Result(url=api_url)\n</code></pre>"},{"location":"reference/environment-variables/#setting-variables","title":"Setting Variables","text":""},{"location":"reference/environment-variables/#docker","title":"Docker","text":"<pre><code>ENV OPENOBSERVE_URL=\"https://observe.example.com\"\nENV OPENOBSERVE_ORG=\"production\"\n</code></pre>"},{"location":"reference/environment-variables/#docker-compose","title":"Docker Compose","text":"<pre><code>services:\n  my-service:\n    environment:\n      OPENOBSERVE_URL: https://observe.example.com\n      OPENOBSERVE_ORG: production\n</code></pre>"},{"location":"reference/environment-variables/#kubernetes","title":"Kubernetes","text":"<pre><code>containers:\n- name: my-service\n  env:\n  - name: OPENOBSERVE_URL\n    value: \"https://observe.example.com\"\n  - name: OPENOBSERVE_TOKEN\n    valueFrom:\n      secretKeyRef:\n        name: observability\n        key: token\n</code></pre>"},{"location":"reference/environment-variables/#command-line","title":"Command Line","text":"<pre><code>export OPENOBSERVE_URL=\"https://observe.example.com\"\nexport OPENOBSERVE_ORG=\"production\"\npython my_service.py\n</code></pre>"},{"location":"reference/environment-variables/#see-also","title":"See Also","text":"<ul> <li>Observability Guide</li> <li>Deployment Guide</li> </ul>"},{"location":"reference/glossary/","title":"Glossary","text":"<p>Key terms and concepts used in the IVCAP Service SDK.</p>"},{"location":"reference/glossary/#artifact","title":"Artifact","text":"<p>A file or data object stored in the IVCAP platform. Services download artifacts as input and upload artifacts as results.</p>"},{"location":"reference/glossary/#batch-service","title":"Batch Service","text":"<p>A service that processes discrete jobs asynchronously. Each job is independent and runs to completion.</p>"},{"location":"reference/glossary/#jobcontext","title":"JobContext","text":"<p>The context object passed to your job processor function. Provides access to job metadata, progress reporting, and platform APIs.</p>"},{"location":"reference/glossary/#service","title":"Service","text":"<p>A reusable, containerized application registered with IVCAP that processes jobs. Defined by a <code>Service</code> class with metadata.</p>"},{"location":"reference/glossary/#service-composition","title":"Service Composition","text":"<p>Building complex workflows by composing multiple services together. One service calls another service during job processing.</p>"},{"location":"reference/glossary/#collection","title":"Collection","text":"<p>A logical grouping of artifacts in the IVCAP platform. Used for organizing related results.</p>"},{"location":"reference/glossary/#event","title":"Event","text":"<p>A message sent during job execution to report progress, errors, or other status information.</p>"},{"location":"reference/glossary/#step","title":"Step","text":"<p>A named phase of job processing. Steps are used to track progress and report metrics.</p>"},{"location":"reference/glossary/#progress-reporting","title":"Progress Reporting","text":"<p>The mechanism for services to communicate job status, progress, and metrics back to IVCAP. Implemented via the event system.</p>"},{"location":"reference/glossary/#requestresult-models","title":"Request/Result Models","text":"<p>Pydantic models that define the input (Request) and output (Result) schemas for your service.</p>"},{"location":"reference/glossary/#openobserve","title":"OpenObserve","text":"<p>An observability platform used for log aggregation, metrics collection, and tracing.</p>"},{"location":"reference/glossary/#opentelemetry","title":"OpenTelemetry","text":"<p>An open standard for exporting logs, metrics, and traces from applications.</p>"},{"location":"reference/glossary/#otlp","title":"OTLP","text":"<p>OpenTelemetry Protocol - the protocol used to export telemetry data to OpenObserve and other collectors.</p>"},{"location":"reference/glossary/#schema","title":"Schema","text":"<p>A JSON Schema definition for the Request or Result model. Describes the structure of data that flows through your service.</p>"},{"location":"reference/glossary/#urn","title":"URN","text":"<p>Uniform Resource Name. IVCAP uses URNs to identify resources (services, artifacts, etc.). Format: <code>urn:ivcap:service:service-id</code></p>"},{"location":"reference/glossary/#see-also","title":"See Also","text":"<ul> <li>API Reference</li> <li>Getting Started</li> </ul>"},{"location":"reference/service-definition/","title":"Service Definition Schema","text":"<p>Reference for the service definition structure.</p>"},{"location":"reference/service-definition/#service-class","title":"Service Class","text":"<p>The <code>Service</code> class defines your service metadata. The <code>contact</code> and <code>license</code> fields use dedicated typed models (<code>ServiceContact</code> and <code>ServiceLicense</code>) for clarity and type safety.</p> <pre><code>from ivcap_service import Service, ServiceContact, ServiceLicense\n\nservice = Service(\n    name=\"My Service\",\n    contact=ServiceContact(name=\"John Doe\", email=\"john@example.com\"),\n    license=ServiceLicense(name=\"MIT\", url=\"https://opensource.org/license/MIT\"),\n)\n</code></pre>"},{"location":"reference/service-definition/#servicecontact-class","title":"ServiceContact Class","text":"<p>Typed model describing the service contact person.</p> <pre><code>from ivcap_service import ServiceContact\n\ncontact = ServiceContact(\n    name=\"Jane Smith\",\n    email=\"jane@example.com\",\n    url=\"https://example.com/jane\",   # optional\n)\n</code></pre>"},{"location":"reference/service-definition/#fields","title":"Fields","text":"Field Type Required Description <code>name</code> <code>str</code> \u2705 Full name of the contact person <code>email</code> <code>str</code> \u2705 Email address of the contact person <code>url</code> <code>str \\| None</code> \u274c Optional URL for the contact (e.g. profile page)"},{"location":"reference/service-definition/#servicelicense-class","title":"ServiceLicense Class","text":"<p>Typed model describing the service license.</p> <pre><code>from ivcap_service import ServiceLicense\n\nlicense = ServiceLicense(\n    name=\"MIT\",\n    url=\"https://opensource.org/license/MIT\",   # optional\n)\n</code></pre>"},{"location":"reference/service-definition/#fields_1","title":"Fields","text":"Field Type Required Description <code>name</code> <code>str</code> \u2705 License name (e.g. MIT, Apache-2.0, proprietary) <code>url</code> <code>str \\| None</code> \u274c Optional URL pointing to the full license text"},{"location":"reference/service-definition/#service-class-fields","title":"Service Class Fields","text":""},{"location":"reference/service-definition/#required","title":"Required","text":"<ul> <li>name (<code>str</code>) \u2014 Human-readable service name</li> <li>contact (<code>ServiceContact</code>) \u2014 Typed contact details (see above)</li> </ul>"},{"location":"reference/service-definition/#optional","title":"Optional","text":"<ul> <li>version (<code>str | None</code>) \u2014 Service version; defaults to the <code>VERSION</code> environment variable if not provided</li> <li>license (<code>ServiceLicense | None</code>) \u2014 Typed license information (see above)</li> </ul>"},{"location":"reference/service-definition/#complete-example","title":"Complete Example","text":"<pre><code>import os\nfrom ivcap_service import Service, ServiceContact, ServiceLicense\n\nservice = Service(\n    name=\"Image Processor\",\n    version=os.environ.get(\"VERSION\", \"1.0.0\"),\n    contact=ServiceContact(\n        name=\"Image Team\",\n        email=\"images@example.com\",\n    ),\n    license=ServiceLicense(\n        name=\"MIT\",\n        url=\"https://opensource.org/license/MIT\",\n    ),\n)\n</code></pre>"},{"location":"reference/service-definition/#generated-schema","title":"Generated Schema","text":"<p>When you run:</p> <pre><code>python my_service.py --print-service-description\n</code></pre> <p>The SDK generates a JSON schema like:</p> <pre><code>{\n  \"name\": \"Image Processor\",\n  \"contact\": {\n    \"name\": \"Image Team\",\n    \"email\": \"images@example.com\"\n  },\n  \"license\": {\n    \"name\": \"MIT\",\n    \"url\": \"https://opensource.org/license/MIT\"\n  },\n  \"input_schema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"image_id\": {\"type\": \"string\"},\n      \"width\": {\"type\": \"integer\"},\n      \"height\": {\"type\": \"integer\"}\n    },\n    \"required\": [\"image_id\"]\n  },\n  \"output_schema\": {\n    \"type\": \"object\",\n    \"properties\": {\n      \"processed_id\": {\"type\": \"string\"}\n    }\n  }\n}\n</code></pre> <p>This schema is used by IVCAP for service discovery and validation.</p>"},{"location":"reference/service-definition/#see-also","title":"See Also","text":"<ul> <li>Service API</li> <li>Job Processing</li> </ul>"}]}